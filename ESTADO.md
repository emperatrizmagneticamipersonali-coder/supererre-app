# ESTADO — SuperErre
Última actualización: 2026-08-30 | Sesión actual: 6/7

⏸️ CHECKPOINT — Sesión 6/7. **AUDITORÍA LEGAL completa (doctrina 47-LEGAL-FISCAL-Y-PRIVACIDAD), cerrada con OK del usuario.**
**Datos del responsable (dados por el usuario, no inventados)**: Ana Karen Dorantes Díaz, persona física/independiente, opera desde México. Contacto legal real: `supererre.app@gmail.com` (reemplazó `hola@supererre.app`, que no era un correo real todavía — se actualizó en Footer.tsx y las 3 páginas legales).
**Inventario real (Paso 1) — lo que la app hace de verdad, verificado en código**: pago único $19.99 (NO suscripción, confirmado en `terminos` viejo y en el copy), garantía de 7 días coherente entre landing (`Garantia.tsx`) y `/reembolso` (sin discrepancia), audio/video 100% on-device (confirmado, nunca sale del dispositivo), CERO uso de IA de pago (el detector de voz es Web Audio API local, no hay `ai_calls`/proveedor externo), CERO analytics/cookies de terceros (ni PostHog ni GA4 ni píxeles — solo cookies de sesión esenciales de Supabase para el login). Por eso, aplicando la doctrina, **NO se forzaron** 3 secciones enteras: transferencia internacional a IA (no aplica, no hay IA), banner de cookies (no aplica, no hay cookies no esenciales), capa legal de suscripción completa (no aplica, es pago único — se dejó explícito en las 3 páginas para que no genere confusión).
**2 huecos reales encontrados y CORREGIDOS (no solo texto — código real)**:
1. La Política de Privacidad prometía "puedes pedir borrar tu cuenta" pero esa función NO existía en el panel — se agregó `eliminarUsuario()` en `app/admin/(panel)/usuarios/actions.ts` + botón "Eliminar" con confirmación en `UsuariosClient.tsx`. Se verificó el esquema real de Supabase antes de escribirla: `parents_id_fkey`, `children_parent_id_fkey`, `purchases_parent_id_fkey` y `exercise_progress_child_id_fkey` ya tienen `ON DELETE CASCADE` (de una migración anterior) — así que `admin.auth.admin.deleteUser()` solo, sin queries manuales, borra cuenta+hijos+progreso+compras en cascada. `event_log`/`error_log` tienen `ON DELETE SET NULL` (no se borran, quedan anonimizados) — decisión de diseño ya correcta, no se tocó.
2. El formulario de acceso (`LoginForm.tsx`) no tenía ninguna casilla de aceptación de Términos/Privacidad antes de continuar — se agregó (checkbox NO premarcado, con links reales, bloquea el botón de envío hasta marcarla).
**Reescritas las 3 páginas legales** (`privacidad`, `terminos`, `reembolso`) con: nombre/país reales del responsable, subprocesadores NOMBRADOS con función (Supabase/Vercel/Hotmart — sin proveedor de IA), terminología ARCO (México/LFPDPPP) en vez de genérico tipo GDPR, aclaración explícita de "sin suscripción, sin cobros recurrentes" en las 3, aviso de cambios materiales por correo, límite de responsabilidad ampliado en Términos, ley aplicable = México con salvedad de derechos de consumidor del país del comprador.
tsc + build limpios (28 rutas).
Próximo paso: el usuario revisa las 3 páginas publicadas y prueba el botón "Eliminar" con un usuario de prueba (NO con su propia cuenta admin — está bloqueado a propósito). Pendiente marcado para más adelante, no urgente hoy: validación por abogado local antes de escalar en serio (ya está la advertencia en las propias páginas).

⏸️ CHECKPOINT — Sesión 6/7 CERRADA por el usuario (noche, "seguimos mañana").

⏸️ CHECKPOINT — Sesión 6/7. **Bug real encontrado y corregido: el enlace de "agregar usuario manual" confirmaba la cuenta en Supabase pero no tenía dónde aterrizar.** El límite de correos de ayer sí se reseteó solo (como se esperaba) — la invitación a un segundo usuario de prueba llegó bien y `auth.users` confirma `confirmed_at`/`email_confirmed_at` seteados (la persona SÍ completó el login del lado de Supabase) — pero el click terminaba en "no se puede", porque `crearUsuarioManual()`/`reenviarAcceso()` nunca especificaban `redirectTo`, así que Supabase usaba el Site URL (la raíz `/`) y esa página no tiene ningún código para intercambiar el `code` de la URL por una sesión real — quedaba un token sin consumir y ninguna pantalla que lo recibiera.
**Fix**: nuevo `app/auth/callback/route.ts` (paralelo a `/admin/auth/callback`, pero para usuarios NORMALES — intercambia el código y redirige a `/app`). `crearUsuarioManual` y `reenviarAcceso` ahora pasan `redirectTo`/`emailRedirectTo` explícito apuntando ahí, calculado del propio request (`headers()` → host/proto) en vez de depender de otra variable de entorno más que el usuario tendría que configurar a mano en Vercel.
⚠️ **Límite honesto que sigue existiendo, documentado para no prometer de más**: aunque el login ahora aterriza bien en `/app`, esa parte de la app sigue siendo 100% localStorage — no hay ningún vínculo real entre "esta persona tiene una cuenta de Supabase" y "esto es lo que ve en `/app`". El usuario invitado va a entrar y ver la app como cualquier visitante nuevo (onboarding desde cero), no datos ligados a su cuenta. Conectar `/app` a cuentas reales es trabajo aparte, no pedido todavía — se avisó explícitamente para no generar una falsa expectativa.
tsc + build limpios (17→28 rutas).
Próximo paso: confirmar con el usuario que el enlace de invitación ahora sí lo lleva a `/app` sin error (probar de nuevo con Ana, o con "reenviar acceso" desde el panel de Usuarios).

⏸️ CHECKPOINT — Sesión 6/7 CERRADA por el usuario (noche, "seguimos mañana"). **El panel de administración quedó verificado en vivo por el usuario, funcionando de punta a punta.** Confirmado con captura real de `/admin` (Resumen): banner "✅ Todo en orden por ahora", Usuarios totales: 1 (el admin), Ventas/Actividad diciendo honestamente "Sin datos todavía", Errores: 0. Es el primer cierre de esta sesión con verificación visual REAL (no de este entorno — del propio usuario en su celular).
**Pendiente encontrado al final, diagnosticado con la causa exacta (logs de Supabase), no resuelto todavía porque se resuelve solo:** al probar "agregar usuario manual" (Ana → segundo usuario de prueba), Supabase devolvió `429 over_email_send_rate_limit` en `/invite` — el servicio de correo GRATIS y compartido de Supabase (`noreply@mail.app.supabase.io`, pensado solo para pruebas chicas) se saturó por la cantidad de correos que mandamos hoy mismo probando el login (varios magic links + la invitación inicial). Confirmado vía `query_logs` (`auth_logs`), no es un bug de `crearUsuarioManual()` — el código intentó todo correctamente. **Se resetea solo en un rato (normalmente <1h)** — no hace falta tocar nada, solo reintentar más tarde.
⚠️ **Pendiente real para más adelante (no urgente, anotado para no perder de vista)**: para uso en producción con compradores de verdad, hace falta conectar un proveedor de correo real (Resend, ya está en el plan del SO — ver `18-VENTA-HOTMART.md`/skill `emails`) en vez de depender del correo por defecto de Supabase, que SIEMPRE va a tener este mismo límite bajo. Sin esto, el día que haya varias altas de usuarios reales el mismo día, van a chocar con el mismo `429`.
**Riesgo de robustez del checkpoint anterior sigue sin resolver** (ver checkpoint previo): `proxy.ts` corre en TODAS las rutas y no tiene manejo defensivo si Supabase falla — sigue siendo una mejora pendiente, no crítica mientras las env vars de Vercel estén bien puestas (ya lo están, verificado hoy).
Próximo paso exacto: mañana, esperar que pase el rate limit y volver a probar "agregar usuario manual" (con Ana u otro correo de prueba) para confirmar que ESA parte también funciona de punta a punta — es lo único de la Capa 2 que quedó sin verificar hoy. Después, seguir recorriendo Usuarios/Uso/Errores/Negocio con el usuario y decidir cuándo conectar Hotmart de verdad.

⏸️ CHECKPOINT — Sesión 6/7. **Panel de administración: publicado en producción y el usuario ya entró con éxito.** Cerró toda la saga de configuración que quedó pendiente del checkpoint anterior. Nada de código cambió en esta ronda — todo fue configuración externa que el usuario hizo él mismo (con guía paso a paso, nunca pasando secretos por el chat):
1. **`Authentication → URL Configuration` en Supabase estaba con el default de localhost** — el primer enlace mágico llevaba a `http://localhost:3000/...`, de ahí el "no se puede acceder a este sitio". Corregido: Site URL y Redirect URLs apuntando a `https://supererre-app.vercel.app`.
2. **Las 3 variables de entorno nunca se habían agregado en Vercel** (solo existían en el `.env.local` local) — sin ellas, el `proxy.ts` (que corre en CADA request, no solo `/admin`) tiraba error al no poder crear el cliente de Supabase → **toda la app estuvo caída (Internal Server Error) en producción**, no solo el panel. Se agregaron las 3 en Vercel → Environment Variables (marcadas Production) + Redeploy manual.
3. Fricciones menores en el camino (anotadas por si se repiten): el traductor de Chrome rompe el dashboard de Supabase (usar incógnito o "Mostrar original"); la cuenta de Supabase del usuario se creó por login directo de correo, no por contraseña propia ("This account does not support password sign-in"); Vercel exige elegir tipo Secreto/Configuración al guardar cada variable (cualquiera de las dos funciona igual de bien para que la app arranque, la diferencia es solo si se puede volver a leer el valor después).
⚠️ **Riesgo real identificado, no resuelto todavía**: el `proxy.ts` corre en TODAS las rutas (matcher excluye solo estáticos) y no tiene manejo defensivo si `createServerClient` falla — si las env vars de Supabase alguna vez se borran o vencen en Vercel, la app ENTERA se cae de nuevo (no solo `/admin`), tal como pasó hoy. Sería más robusto que el proxy fallara silencioso (dejar pasar la request sin sesión) en vez de tirar un 500 que tumba la landing/onboarding/paywall. No se tocó en esta ronda por no ser lo pedido — queda anotado como mejora de robustez pendiente si se quiere blindar contra esto a futuro.
**Preferencia nueva del usuario, guardada en memoria**: dar toda instrucción paso a paso en español Y en inglés (los paneles de Supabase/Vercel a veces le aparecen en inglés aunque hablemos en español).
Próximo paso: el usuario va a recorrer cada sección del panel (Ventas/Usuarios/Uso/Errores/Negocio) y contar qué ve — varias secciones van a decir "Sin datos todavía", que es el comportamiento esperado, no un error.

⏸️ CHECKPOINT — Sesión 6/7. **PANEL DE ADMINISTRACIÓN — Capas 0 a 2 construidas y verificadas (`/admin`).** Pedido explícito del usuario, con plan presentado y aprobado antes de construir. Decisión tomada con el usuario (AskUserQuestion): Ventas/Ganancia/Negocio quedan con queries reales pero "Sin datos" hasta conectar Hotmart (sesión aparte); login de admin por enlace mágico.
**Hallazgo clave al arrancar**: la app NUNCA se había conectado a Supabase en código (0 dependencias de `@supabase/*`, 0 usos en el repo) pese a que las tablas `parents/children/exercise_progress/purchases` existían desde una sesión anterior — y el proyecto de Supabase estaba PAUSADO por falta de uso. Se reactivó (`restore_project`) y se conectó la app de verdad por primera vez. Tampoco existe todavía ningún webhook de Hotmart, ni la app llama a ninguna IA de pago (el detector de voz es 100% local/gratis) — por eso NO hay sección de "costo de IA": no aplica a este producto, se lo aclaré al usuario en el plan en vez de inventar una sección vacía para siempre.
**Capa 0 — Cimientos:**
- Instalado `@supabase/supabase-js` + `@supabase/ssr` + `server-only` + `zod` (dependencias reales, antes solo `zod` vivía como transitiva).
- Migraciones aplicadas en Supabase (proyecto `tpjvjlzrigxnvluhpzep`): `parents` ganó `role` ('user'|'admin'), `name`, `source`, `status`; función `private.is_admin()` (`security definer`, evita recursión de RLS) + políticas `admin_reads_*` en parents/children/purchases; tablas nuevas `event_log`, `error_log`, `acquisition_spend` con RLS (solo admin lee); `purchases` ganó `amount_minor`/`currency`/`is_afiliado`/`ciclo` (no existía NINGUNA columna de monto — imposible calcular ingresos sin esto, se agregó vacía para cuando el webhook exista); rate limiting canónico de 09-SEGURIDAD (`private.rate_limit_hits` + `check_rate_limit()`) para los 2 endpoints públicos de logging.
- **Hallazgo de seguridad corregido de paso**: `handle_new_parent()` (trigger de una sesión anterior) quedaba ejecutable directo vía RPC por `anon`/`authenticated` (lo marcó el advisor de seguridad de Supabase) — se revocó ese permiso público, es un trigger interno, no una función pública.
- `lib/supabase/{client,server,admin,middleware}.ts` (patrón canónico de 26-AUTH-MODERNO) + `proxy.ts` en la raíz (Next.js 16 renombró `middleware.ts` → `proxy.ts`; el codemod oficial pedía el repo sin cambios pendientes para correr, así que se hizo el rename a mano — mismo resultado). **Alcance deliberado**: el proxy/middleware protege SOLO `/admin` — `/app` (papás/niños) sigue sin auth real a propósito, eso es trabajo aparte todavía no pedido.
- `.env.local` creado con las 2 claves públicas (URL + publishable, no son secretas) ya completas; `SUPABASE_SECRET_KEY` la configuró el usuario él mismo en su máquina, sin pasar por el chat (costó varias vueltas: primero pegó texto en una ventana de Notas sin guardar, después la clave pública en vez de la secreta — la clave `service_role` en Supabase viene tapada detrás de un botón "Reveal" que es fácil pasar por alto).
**Capa 1 — Acceso de dueño:** `/admin/login` (enlace mágico, `shouldCreateUser:false` — este login nunca crea cuentas nuevas) + `/admin/auth/callback` (intercambia el código PKCE por sesión) + `lib/admin/auth.ts` (`requireAdmin()`, se re-verifica en cada página/Server Action, no solo en el layout — defensa en profundidad real, no solo esconder la ruta). Cuenta admin creada para `diazanni0295@gmail.com` (único admin) vía script puntual con la service key del usuario (nunca vista por mí), ya borrado.
**Capa 2 — Usuarios (100% real desde hoy):** lista real (cruza `auth.users` vía Admin API + `parents`), buscador, alta manual con nombre+email+plan (`inviteUserByEmail`, Server Action con zod), activar/desactivar, reenviar acceso (magic link nuevo). 0 usuarios reales todavía aparte del admin — la tabla dice "Sin datos todavía" en vez de inventar filas.
**Capa 3 — Uso (real desde hoy, aunque en 0 por ahora):** `event_log`/`error_log` alimentados por 3-4 eventos reales ya cableados en `lib/progress.ts` (`onboarding_completado`, `app_abierta`, `ejercicio_completado` ×3) + `sesion_iniciada` (dedup diario, `components/app/SesionTracker.tsx` en `app/app/layout.tsx`) vía `lib/analytics.ts` → `/api/log-event` (público a propósito, con rate limit — la app no tiene login real de compradores todavía, se identifica por `device_id` anónimo en localStorage, sin PII). Retención D1/D7/D30 calculada de verdad (por dispositivo) en `lib/admin/data.ts`, solo cuenta cohortes con tiempo suficiente para medirse.
**Capa 4 — Errores:** `app/global-error.tsx` nuevo (no existía NINGÚN Error Boundary en toda la app pese a la regla 18 de CLAUDE.md — mínimo indispensable agregado acá, no un retrofit completo por sección) → `/api/log-error` → panel agrupa por frecuencia.
**Capa 5 — Ventas/Ganancia/Negocio:** pantallas y queries reales contra `purchases`/`acquisition_spend` (vacías), dicen "Sin datos todavía — falta conectar Hotmart" en vez de fabricar números. Quedó así a propósito (decisión del usuario).
tsc + build limpios (17→27 rutas) en cada capa. **NO verificado visualmente en este entorno** (limitación de siempre) — falta que el usuario entre de verdad a `/admin` con su enlace mágico y confirme cómo se ve.
⚠️ **Pendiente crítico para que funcione en producción**: el usuario debe agregar las mismas 3 variables (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`) en Vercel → Settings → Environment Variables — hoy solo existen en su `.env.local` local, el panel desplegado en producción va a fallar (fail-closed a propósito) sin esto.
Próximo paso: usuario revisa su correo, entra a `/admin`, prueba cada sección; agregar las env vars en Vercel; decidir cuándo conectar el webhook de Hotmart real (quedó para otra sesión).

⏸️ CHECKPOINT — Sesión 6/7. **Vista previa + botón de comprar unificado para los 10 accesorios (fuera del modo calibración, en la Tienda normal).** El usuario pidió poder ver cómo queda CUALQUIER accesorio (bloqueado o no) al tocarlo, y que todos (sombrero/gafas/capa/corona + los 6 de tienda) tengan un botón de comprar real que descuente monedas.
`app/app/premios/page.tsx`:
- Nueva función `elegirAccesorio(a, yaLoTiene)`: si ya es tuyo, lo pone/saca de una (como siempre); si NO, lo muestra en **vista previa** sobre el retrato del León (`previsualizado`, estado nuevo, independiente de `p.accesorioEquipado` — no se guarda nada todavía) Y abre el diálogo de compra al mismo tiempo, para que el niño vea cómo le queda ANTES de decidir comprarlo.
- Se quitó el `disabled={!lograda}` de la grilla "Tus logros" — ahora CUALQUIER accesorio (logrado o no) se puede tocar; los bloqueados se ven atenuados con un candadito pequeño en la esquina en vez de reemplazar la imagen entera por el candado (así el niño ya ve qué es antes de desbloquearlo).
- El diálogo de compra ahora dice "Así se ve puesto" y, si no alcanzan las monedas, muestra "Te faltan X monedas todavía" con el botón "Sí, comprar" deshabilitado (antes fallaba en silencio si no alcanzaba). `comprarAccesorio()` (ya existía en `progress.ts`) sigue siendo la única función que descuenta monedas de verdad — no se tocó su lógica, solo se expuso mejor en la UI.
- Cancelar el diálogo limpia la vista previa (vuelve a mostrar lo que el niño tiene puesto de verdad).
- El modo calibración (`?calibrar=1`) sigue funcionando exactamente igual que antes — la vista previa nueva es un camino aparte que solo se usa fuera de calibración.
tsc + build limpios (17/17, el build tardó ~8min esta vez, el sistema estaba lento pero terminó bien).
Próximo paso: que el usuario pruebe tocar un accesorio bloqueado (ej. la Corona) en la Tienda normal (sin `?calibrar=1`) y confirme que lo ve puesto en el León + le aparece el precio para comprarlo, y que probar comprar de verdad le descuenta las monedas.

⏸️ CHECKPOINT — Sesión 6/7. **2 fixes tras feedback del usuario probando la capa/corona en modo calibración:**
1. **El recorte de la patilla de las gafas (checkpoint anterior) se pasó de largo y le cortó un pedazo real al lente** — el usuario avisó "se ven incompletos". Diagnóstico correcto esta vez: medí fila por fila (no un único corte vertical) el punto exacto donde el aro circular termina y empieza el brazo — el aro sube hasta x≈143-144px de ancho en las filas de arriba/abajo del brazo, pero mi corte anterior había cortado en x=133, mordiendo el círculo. Recorté de nuevo desde el PNG original guardado en git (commit `d92d2ae`, antes del primer recorte) y esta vez solo borré el tramo x≥148 en las filas y=88-115 (donde de verdad está el brazo largo, que llega hasta x=188) — el círculo del lente quedó completo.
2. **"la app no me deja checar para ver realmente cómo queda la capa y la corona"** — hipótesis más probable: la cuadrícula roja de referencia (`CuadriculaCalibracion`) tapa la vista limpia del personaje mientras se calibra. Se agregó un botón "Ver limpio"/"Ver cuadrícula" dentro del panel de modo calibración (`verCuadricula`, nuevo estado) para poder alternar y ver el resultado sin líneas encima, sin salir del modo calibración ni perder los sliders. ⚠️ Sigue pendiente confirmar con el usuario si esto era lo que quería decir, o si el problema era otra cosa (por ejemplo, que capa/corona siguen bloqueadas FUERA de `?calibrar=1` hasta ganarlas o comprarlas — eso es comportamiento esperado, no bug).
tsc + build limpios (17/17).
Próximo paso: esperar aclaración/confirmación del usuario sobre el punto 2, y que revise si el lente de las gafas ya se ve completo.

⏸️ CHECKPOINT — Sesión 6/7. **Los 4 accesorios de logro (sombrero/gafas/capa/corona) ya tienen posición VERIFICADA por el usuario en su celular real — cierra el bug que llevaba 3 rondas.** Con el modo calibración (`?calibrar=1`), el usuario probó los 4 en vivo y mandó captura de cada uno:
- Gafas: `top:14, left:33, ancho:91` — confirmado, se ven de frente sobre los ojos (con la patilla ya recortada del checkpoint anterior).
- Corona: `top:0, left:34, ancho:74` — confirmado, apoyada bien sobre la cabeza.
- Capa Dorada: `top:44, left:34, ancho:89` — el usuario aclaró que la posición/tamaño están bien, pero pidió que se vea "por detrás del cuerpo del León" (se veía como un babero por delante, no como una capa puesta en la espalda). **Fix real, no solo de coordenadas**: `Accesorio` tiene un campo nuevo `detras?: boolean` (`lib/accesorios-data.ts`, solo `capa` lo tiene en `true`); en `app/app/premios/page.tsx` el retrato ahora dibuja el `AccesorioOverlay` de la capa ANTES del `<Mascota>` (detrás en el DOM/z-order) y el resto (sombrero/gafas/corona) sigue después (delante), como toda la vida. Como el canvas de `Mascota` solo pinta los píxeles del video (chroma-key deja transparente el resto), la capa asoma por los bordes del cuerpo del León en vez de taparlo.
Los 4 valores ya quedaron fijos en `lib/accesorios-data.ts` con comentarios que dicen "calibrado por el usuario en vivo" (no cálculo, no estimación — la única categoría de este bug que sí cuenta como verificada de verdad). tsc + build limpios (17/17).
Próximo paso: pedirle al usuario que confirme cómo se ve la capa detrás del cuerpo (es el único de los 4 sin captura post-fix) — si no queda bien el `top`/`left`/`ancho` puede necesitar un pequeño reajuste ahora que cambió de plano, pero la lógica de "por detrás" en sí ya está resuelta.

⏸️ CHECKPOINT — Sesión 6/7. **Modo calibración ahora permite probar Capa Dorada y Corona sin tenerlas desbloqueadas.** El usuario reportó que le aparecían "bloqueadas" al querer calibrarlas — es esperado (son premios de logro por completar toda una sección, o comprables por 250/300 monedas, y todavía no llegó ahí) pero bloqueaba poder probarlas. Fix en `app/app/premios/page.tsx`: con `calibrando` activo, `lograda`/`tenido` se fuerzan a `true` SOLO para efectos de mostrar/habilitar el botón y poder equipar en vista previa (`equiparAccesorio` no toca monedas ni `accesoriosComprados`, es 100% seguro de usar así) — el arrastre (`onPointerDown`→`iniciarArrastre`) se desactiva en calibración para no interferir con el tap. Fuera del modo calibración (usuario normal, sin el query param) el comportamiento real de desbloqueo/compra no cambió en nada. tsc + build limpios (17/17).
Próximo paso: que el usuario calibre Capa Dorada (cuerpo/espalda) y Corona (cabeza) con los mismos 3 sliders y mande captura de cada una.

⏸️ CHECKPOINT — Sesión 6/7. **Gafas: la patilla larga se veía saliendo por detrás de la oreja del León — corregido editando la IMAGEN, no la posición.** El usuario calibró top/left/ancho con el modo `?calibrar=1` (14/33/91) y quedaron bien puestas, pero el arte de `gafas.png` está dibujado en 3/4 con la patilla derecha larga y muy visible (no de frente) — eso es lo que sobresalía. Medí en píxeles dónde terminaba el aro del lente derecho (x≈132 de 200) y borré (alpha=0) todo lo que seguía a la derecha de ahí directo en el PNG con PowerShell/System.Drawing — la patilla y el gancho de la oreja desaparecieron, el lente quedó intacto y redondo. Como el recorte es solo transparencia agregada AL COSTADO del lienzo (no se movió ni escaló el contenido existente), el top/left/ancho que el usuario ya había calibrado sigue siendo válido — no hace falta que los vuelva a ajustar. tsc + build limpios (17/17).
Próximo paso: confirmar con el usuario que las gafas ya se ven de frente, y seguir con Capa Dorada y Corona por el mismo modo de calibración.

⏸️ CHECKPOINT — Sesión 6/7. **¡Funcionó! El modo calibración resolvió el sombrero a la primera.** El usuario abrió `/app/premios?calibrar=1`, movió los 3 sliders hasta que el sombrero le quedó bien puesto (mandó captura confirmando visualmente), y los valores finales que encontró (`top:0, left:25, ancho:88`) quedaron fijos en `lib/accesorios-data.ts` — **son valores VERIFICADOS visualmente de verdad** (por el usuario, en su celular real), a diferencia de los 3 intentos anteriores que eran cálculo/estimación sin poder ver el resultado. Nota interesante: el valor real (`left:25`) NO coincide con el que había calculado geométricamente en la ronda anterior (`left:50`, derivado de la matemática de "contain" del video) — confirma que había algún supuesto incorrecto en ese cálculo (probablemente el video no está perfectamente centrado como asumí, o algo en el layout real difiere), y que la calibración en vivo era el camino correcto, no seguir afinando la fórmula a ciegas. tsc + build limpios (17/17).
Próximo paso: esperar que el usuario haga lo mismo con gafas, capa y corona (mismo modo calibración) y mande los 3 números de cada una.

⏸️ CHECKPOINT — Sesión 6/7. **Cambio de estrategia en el bug del sombrero/gafas/capa/corona: el usuario confirmó que el 3er ajuste "a ciegas" TAMPOCO quedó bien.** Tres rondas de calibrar desde video/medición y ninguna funcionó — se decidió dejar de adivinar y darle al usuario una herramienta para que calibre él mismo, en su celular real, viendo el resultado en vivo (lo que este entorno nunca pudo hacer).
**Nuevo: "modo calibración" en `/app/premios?calibrar=1`** (`app/app/premios/page.tsx`, wrapper `<Suspense>` nuevo por el `useSearchParams()`, mismo patrón que Praxias/Sonidos/Escalera): con el parámetro activo y un accesorio equipado, aparece (a) una cuadrícula roja cada 10% superpuesta al retrato del León, y (b) un panel con 3 deslizadores (top/left/ancho) que mueven el accesorio EN VIVO sobre el retrato real, más una línea de texto `top: X, left: Y, ancho: Z` para que el usuario me la copie/mande por captura. Es aditivo y solo visible con el query param — no cambia nada del flujo normal para el niño.
**Bug separado también corregido**: "en los accesorios no aparece para comprar, sino que me dice cómo buscar la palabra" — diagnóstico: los `<span>` con el nombre y el precio de cada accesorio de la tienda NO tenían `select-none`, así que un toque un poco sostenido en celular (común cuando el `onPointerDown` del arrastre compite con el `onClick` de comprar) se interpretaba como selección de texto, y Android/Chrome ofrece "Buscar '<texto>' en Google" — de ahí el mensaje confuso. Se agregó `select-none` a esos 2 spans + `WebkitTouchCallout:"none"` al botón (bloquea también el menú de "copiar/compartir" de iOS Safari en long-press).
tsc + build limpios (17/17) en ambos cambios.
⚠️ Sigue sin verificación visual en este entorno (de nuevo, el panel de preview no compone frames) — pero esta vez la verificación la hace el usuario directo, en vivo, con datos exactos de su propio celular, así que debería ser la última ronda de este bug.
Próximo paso: pedirle al usuario que abra el link con `?calibrar=1`, pruebe el modo calibración con cada uno de los 4 accesorios (sombrero/gafas/capa/corona) y mande los 3 números finales de cada uno; con esos, dejar los valores fijos en `lib/accesorios-data.ts` y sacar el modo calibración (o dejarlo, es inofensivo). También confirmar que ya no aparece el menú de "buscar la palabra" en la tienda.

⏸️ CHECKPOINT — Sesión 6/7. **Sombrero recalibrado con medición real (no a ojo) tras un tercer video del usuario mostrándolo tapando toda la cara del León.** Esta vez se midió en serio en vez de ajustar por evidencia visual aproximada: se extrajo un frame real de `leon.mp4` (720×1280) con ffmpeg, se ubicaron cejas/orejas/ancho de melena con una cuadrícula de referencia superpuesta (PowerShell+System.Drawing), y se tradujo todo a % del retrato de 200px reproduciendo la matemática exacta de "contain" que usa `useVideoChromaKey` (letterbox horizontal de 43.75px por lado, sin offset vertical). También se midió la caja alfa real del PNG del sombrero (contenido centrado, ocupa 89%×61% de su lienzo cuadrado) para saber cuánto sobresale el ala respecto al punto `top`/`left` declarado. Con esos dos datos duros se recalculó dónde debía quedar el CENTRO del sombrero para que el ala caiga a la altura de las cejas (apoyado sobre la melena) en vez de sobre los ojos: `top` 14→8, `left` 24→50 (el error más grande: estaba corrido a la izquierda del centro real, que sí coincide con el centro del retrato), `ancho` 110→90. tsc+build corriendo al cierre de este checkpoint.
Revisé también las **gafas** en el mismo video: en los cuadros donde aparecen ya puestas (sin arrastre en curso) caen bien sobre los ojos — la posición actual (`top:38,left:50`) coincide con el cálculo matemático del punto de los ojos (39%), así que NO se tocó. Los cuadros donde se ven flotando lejos de la cara son, con alta probabilidad, el momento en que el usuario las está ARRASTRANDO con el dedo (el visual de arrastre sigue el dedo, no la posición final) — no hay evidencia de que el equipado final esté mal.
⚠️ Sigue siendo cálculo geométrico verificado con medición real de imagen, NO una captura en vivo de la app (mismo límite de entorno de siempre — el panel de preview no compone frames). Es la calibración más rigurosa de las 3 rondas de este bug; si el usuario la prueba y sigue mal, pedir un video corto SOLO del sombrero (sin nada más) para volver a medir.
Próximo paso: esperar confirmación del usuario sobre el sombrero, y preguntar si las gafas también le siguen pareciendo mal posicionadas en el equipado final (no en el arrastre).

⏸️ CHECKPOINT — Sesión 6/7. **Bug crítico corregido: el usuario quedaba trabado en "Primera Victoria" del onboarding (el primer contacto con el micrófono) sin poder pasar, tras varios intentos reales.** tsc + build limpios (17/17), no verificable en vivo (mismo límite de entorno de siempre).
Causa raíz encontrada: `StepVictoria` llamaba `useRugidoDetector()` SIN pasarle `edad` ni `severidad` — a diferencia de Sonidos/Escalera (que sí los reciben), usaba siempre el calibrado genérico de 6 años, ignorando que en ese punto del onboarding YA se conocen ambos datos (se recogen antes, en los pasos edad/dolor). Corregido: se hilan `edad` y `severidad` hasta `StepVictoria`.
Además, en las 3 pantallas de detección (Primera Victoria, Sonidos, Escalera) se agregó:
- **Medidor de volumen real** (barra que se llena con `nivelVoz`) mientras escucha — para poder ver si el micrófono capta algo o no, en vez de adivinar a ciegas una cuarta vez.
- **Escape de seguridad**: si pasan 10s (Primera Victoria) o se acaba el cronómetro (Sonidos/Escalera) sin detectar nada, aparece "¿Sigue sin escucharte? Continuar de todos modos" — nadie vuelve a quedar trabado sin salida, sea cual sea la causa real (ruido de fondo, cancelación de eco agresiva del celular, etc.).
⚠️ Nota de continuidad: esta sesión se reinició a mitad de la verificación (tareas de fondo quedaron huérfanas) — se confirmó que todo el código ya estaba bien guardado en disco antes del corte, solo hubo que re-correr tsc/build desde cero.
Próximo paso: esperar que el usuario confirme que ya puede pasar de la Primera Victoria.

⏸️ CHECKPOINT — Sesión 6/7. Usuario probó Racha+onboarding y confirmó "sí está bien". Mandó 2 capturas de Duolingo pidiendo look "más 3D". **Decisión importante documentada**: se descartó copiar el modo oscuro/neón de Duolingo — ESO ya se había probado y rechazado explícitamente al principio del proyecto (ver trazabilidad en FICHA-ARTE.md: "Racha de León puro, muy Duolingo-genérico" fue descartada). Se acordó con el usuario mantener la paleta cálida y solo adoptar el LENGUAJE de profundidad 3D. tsc + build limpios (17/17).
- **Botones 3D**: nuevas clases `.btn-3d-primary/secondary/accent` en `globals.css` — borde sólido de 4px abajo (color derivado: `--brand-primary-shadow` = el mismo `--brand-primary-hover` ya existente; secondary/accent vía `color-mix` con negro, no hex nuevo) que se "hunde" (`translateY(4px)` + sombra a 0) al tocar — mismo lenguaje que Duolingo, sin salir de la paleta. Reemplazó `shadow-md` en los 22 botones principales de toda la app (14 archivos).
- **Pantalla "Sesión completa del día"** (`SesionCompleta.tsx`, nuevo): mascota + 3 tarjetas (minutos de hoy / racha / monedas totales) + botón "¡Genial!" — mismo espíritu que la pantalla de "lección terminada" que mandó de ejemplo, con nuestros colores. Conectada en las 3 pantallas de práctica (Praxias/Sonidos/Escalera): antes, cuando se acababan los minutos del día, la sesión continua simplemente no hacía nada (gap ya anotado en un checkpoint anterior) — ahora se muestra esta pantalla.
Próximo paso: esperar que el usuario pruebe el efecto 3D de los botones y la nueva pantalla de sesión completa (para verla hay que agotar los minutos del plan diario, o bajarle la edad/probar varios ejercicios seguidos). Sigue pendiente el repaso general de la landing.

⏸️ CHECKPOINT — Sesión 6/7. **Capa 2 (onboarding, 11 pasos) — HECHA, tsc + build limpios (17/17).**
- StepPaywall y StepGate no tenían `animate-fade-up` en su raíz (los únicos 2 pasos sin entrada animada) — agregado.
- Stagger 60ms agregado en: botones de edad (StepEdad), chips de "¿Cómo dice la R hoy?" (StepDolor), chips de interés (StepInteres, pausado/sin usar pero corregido igual por consistencia), checklist del paywall, 3 botones de la puerta de adultos (StepGate). `Chip.tsx` ahora acepta `style` opcional para poder pasarle el delay desde afuera.
- **Bug real encontrado (mismo patrón que ya se había corregido antes)**: el `<video>` de `StepVictoria` (el espejo circular de la Primera Victoria) usaba `className="hidden"` directo, en vez de pasar por `Mascota.tsx` — es el MISMO bug de `display:none` que frena la decodificación de cuadros en Android/Chrome, ya corregido en `Mascota.tsx`/`VideoCompanero.tsx` en una sesión anterior pero que se había quedado sin aplicar acá porque este video es standalone, no usa esos componentes. Corregido con el mismo patrón (opacity:0 + 1px en vez de display:none).
Los demás 9 pasos (Nombre/Edad/Dolor/Reconocimiento/Revelación/Victoria/Celebración) ya tenían buena entrada animada de sesiones anteriores — no se tocaron.
Próximo paso: preguntarle al usuario si sigue con el repaso general de la landing (Garantía/FAQ/Oferta, pendiente) o si prueba primero Racha+Onboarding en su celular.

⏸️ CHECKPOINT — Sesión 6/7. Skill `diseno` invocado de nuevo — usuario pidió Racha + onboarding completo + repaso general. Se acordó arrancar por capas, empezando por Racha. **Capa 1 (Racha) — HECHA, tsc limpio.**
- Tarjeta de racha actual: fondo pasa de `border+surface-primary` a `bg-brand-accent-soft` (mismo patrón "hero card" que ya tiene Mamá), dato héroe `text-4xl→text-5xl` en color `brand-accent`, círculo del ícono ahora sólido (`bg-brand-accent` + ícono `text-txt-on-brand`) para más contraste sobre el fondo suave.
- Entrada escalonada `animate-fade-up` con delays 0/60/120/180/240ms en: título, tarjeta hero, semana de 7 días, fila mejor-racha/ejercicios, tarjeta de nivel.
- Fila mejor-racha/ejercicios: los 2 recuadros alternan `surface-secondary`/`surface-tertiary` (antes los dos iguales, sin profundidad).
- El conteo animado (`useConteo`) ya existía de una capa anterior, no se tocó.
⚠️ **Descubrimiento de entorno importante para futuras verificaciones**: el navegador de prueba de esta sesión nunca "compone frames" (por eso las capturas fallan toda la sesión) — esto también significa que las animaciones basadas en `requestAnimationFrame` (como `useConteo`) se quedan congeladas en su valor inicial (0) en este entorno, aunque el dato real y la lógica sean correctos (verificado leyendo localStorage directo: la racha sembrada de 5 días estaba bien guardada, solo el número en pantalla no avanzaba). No es un bug de la app — es una limitación de poder "ver" rAF en este navegador de pruebas. Verificado: sin errores de consola, estructura de la página (`read_page`) correcta.
Próximo paso: falta build final de esta capa + Capa 2 (onboarding, 11 pasos) — pendiente el OK para seguir. El repaso general de landing queda para después de cerrar 1 y 2.

⏸️ CHECKPOINT — Sesión 6/7. Lote grande a partir de fotos+video reales del usuario — tsc + build limpios (17/17).
**1) Arrastrar para vestir (nuevo, aditivo)**: `useArrastrarParaVestir` en `premios/page.tsx` — Pointer Events (mouse+touch unificado), el niño puede arrastrar cualquier accesorio ya conseguido desde su grilla hasta el retrato del León y soltarlo ahí para ponérselo (hit-test contra el `getBoundingClientRect` del retrato). Tocar (sin arrastrar) sigue funcionando igual que antes — el arrastre es un agregado, no reemplaza nada. `touchAction:"none"` en los botones arrastrables para que el navegador no confunda el gesto con scroll. ⚠️ Importante: esto resuelve CÓMO se pone el accesorio, pero NO la posición final (sigue usando el `top`/`left` fijo de `accesorios-data.ts`, que puede seguir necesitando calibración — ver punto 3).
**2) Sombrero/gafas/capa/corona también comprables con monedas**: ahora tienen `precio` además de `lograda` (dos caminos al mismo premio). Nueva función `accesorioDesbloqueado()` (logrado O comprado) y `ACCESORIOS_COMPRABLES` (los 4 + los 6 de siempre) para la grilla de "Comprar con monedas". Al tocar uno no poseído aparece un diálogo de confirmación con imagen+nombre+"Esto te costará X monedas" antes de descontar — ya no es compra instantánea al toque.
**3) Sombrero mal ubicado — segundo intento, sigue sin verificación en vivo**: el usuario mandó capturas mostrando el sombrero ahora tapando toda la cara del León (mi ajuste anterior de `top:14,left:24,ancho:110` quedó mal). Dado que el arrastre (punto 1) ya resuelve la interacción, decidí NO seguir ajustando a ciegas las coordenadas — dejé los valores como estaban del intento anterior. Si tras la próxima prueba real el sombrero sigue viéndose mal AL SOLTARLO sobre el León, hace falta otro video corto igual que el anterior para recalibrar con medición real, o considerar guardar la posición donde el usuario lo soltó en vez de una posición fija (mejora a futuro, no implementada).
**4) Personalización real por severidad de la R (antes se perdía)**: el paso de onboarding "¿Cómo dice la R hoy?" (`dolor`) armaba un texto bonito ("Punto de partida", etc.) pero NUNCA se guardaba — se descartaba al terminar el onboarding. Ahora: `Progreso.severidadR` nuevo campo, viaja onboarding→URL→login→`iniciarProgreso`. `useRugidoDetector` combina edad Y severidad (`factorSeveridad`): "omisión total" → detector más permisivo (todo intento cuenta), "a veces sí a veces no" → un poco más exigente. Se muestra a los papás en Mamá ("Personalización: Punto de partida" + explicación). ⚠️ Alcance consciente: esto ajusta la EXIGENCIA del detector, no reordena todavía qué ejercicios ve cada quién (la secuencia Praxias→Sonidos→Escalera sigue siendo la misma para todos) — restructurar el CONTENIDO por severidad es un cambio más grande, no hecho en esta ronda.
**5) Recuadro raro en "Construyendo el mundo de..."**: no se encontró la causa exacta sin poder ver el navegador en vivo — se aplicaron 2 fixes preventivos (`position:relative` en el wrapper de Mascota, `preload="auto"` en su video). Puede seguir apareciendo — si es así, pedir otro video corto de ESE momento puntual para diagnosticar con el mismo método de extracción de cuadros que funcionó para el sombrero.
Próximo paso: esperar que el usuario pruebe todo esto — especialmente el arrastre (es la funcionalidad más nueva) y si el recuadro del onboarding desapareció.

⏸️ CHECKPOINT — Sesión 6/7. Ronda con video real del usuario — tsc limpio.
- Monedas por ejercicio: 1→10 (`MONEDAS_POR_EJERCICIO` en `progress.ts`). Precios de la tienda re-escalados x10 (50-150 en vez de 5-15) para que la progresión siga sintiéndose igual, no instantánea.
- Pestaña "Mi personaje" → "Tienda" (a pedido explícito). El sub-encabezado interno que también decía "Tienda" se renombró a "Comprar con monedas" para no repetir la palabra dos veces en la misma pantalla.
- **Bug real encontrado con evidencia en video**: el usuario mandó una grabación de pantalla real mostrando el Sombrero de Explorador flotando a la DERECHA de la cabeza del León, no puesto encima — se veía como un sticker suelto, no como algo que se está vistiendo. Diagnóstico: extraje cuadros del video con ffmpeg (ya estaba instalado de una sesión anterior) y medí en píxeles — la matemática de "contain" del canvas (video 720×1280, vertical) decía que debería quedar centrado, pero la evidencia real mostraba el sombrero muy a la derecha (~78% en vez de 50%). No encontré la causa raíz exacta (no hay navegador real disponible en este entorno para debuggear en vivo), así que se corrigió A OJO desde la medición del video: `top` 8→14, `left` 50→24, `ancho` 120→110.
⚠️ **Esta corrección es una calibración a ciegas, no una verificación** — puede necesitar un segundo ajuste. Los otros 3 accesorios de imagen real (gafas/capa/corona) y los 6 de la tienda (que usan la misma lógica de `top`/`left`/Mascota) probablemente tengan el MISMO problema pero no hay evidencia en video de ellos todavía — no se tocaron para no adivinar sin datos. Si el usuario los prueba y también están mal ubicados, pedirle otro video/fotos de cada uno para calibrar con el mismo método (extraer cuadro + medir en píxeles).
Próximo paso: esperar que el usuario confirme si el sombrero quedó bien, y si hace falta corregir los demás accesorios con el mismo método.

⏸️ CHECKPOINT — Sesión 6/7. Feedback de moneda ganada — HECHO, tsc+build limpios (17/17).
- `components/app/MonedaVolando.tsx` nuevo: 3 iconos de moneda que salen del centro-abajo y "vuelan" hacia arriba-derecha con fade (keyframe `moneda-volar` en `globals.css`, 900ms). Se dispara con un `trigger: number` (timestamp) en vez de booleano, para poder re-disparar aunque el ejercicio anterior también hubiera sido "true".
- `lib/sonidoMonedas.ts` nuevo: sonido de moneda sintetizado con Web Audio (2 tonos ascendentes, sin archivo de audio — no hay forma de generar/conseguir un archivo en este entorno, y esto es 100% local igual que el resto de la app).
- Conectado en los 3 lugares donde se completa un ejercicio: Praxias ("Ya lo intenté"), `SonidoDetector` y `NivelDetector` (Escalera R/L) — mismo patrón en los 3.
- "TIENDA" (sección de comprar accesorios en Premios → Mi personaje) ya decía así desde el checkpoint anterior — el usuario lo pidió de nuevo pero ya estaba, no hizo falta cambiar nada ahí.
Próximo paso: esperar que el usuario lo prueba en su celular — ni la animación ni el sonido se pudieron verificar visualmente/auditivamente en este entorno.

⏸️ CHECKPOINT — Sesión 6/7. **Sesión continua entre secciones — HECHO, tsc+build limpios (17/17).**
- `lib/sesion.ts` nuevo: `pasosDeSesion(p, tema)` — lista plana de Praxias+Sonidos+Escalera R+Escalera L en el mismo orden/desbloqueo secuencial que ya usa el Mapa (sin Memorama, es un juego aparte). `siguientePasoSesion()` devuelve el próximo pendiente después de uno dado.
- `Progreso` nuevo: `segundosHoy`+`fechaSegundosHoy` (se resetea solo al cambiar el día) + `registrarTiempoPracticado()`/`segundosRestantesHoy()` en `progress.ts`, usando `planDiarioPorEdad` como tope.
- Praxias/Sonidos/`EscaleraLetra` (sirve a R y L) ahora leen `?ex=id` de la URL para abrir un ejercicio puntual al llegar desde otra sección, y en el momento de "Reclamar mi premio" (fin de cada nivel/peldaño) — DESPUÉS de que se cierran los premios en cola — chequean el tiempo restante y si alcanza, `router.push()` directo al siguiente paso global. Si ya no queda tiempo, se queda en la lista de esa sección (no hay pantalla de "sesión terminada" dedicada todavía — simplificación consciente por tiempo).
- ⚠️ Requirió agregar `<Suspense>` en las 4 pantallas (Praxias, Sonidos, Escalera, Escalera-L) porque Next.js exige envolver `useSearchParams()` para poder generar la página estática — el primer build falló con "should be wrapped in a suspense boundary", se corrigió separando cada página en un wrapper `<Suspense>` + un componente interno con la lógica real. Patrón a repetir si se agregan más `useSearchParams()` en el futuro.
- Alcance consciente: el chequeo de presupuesto de tiempo pasa en los "cortes naturales" (fin de nivel de Praxias, fin de un modo de Sonidos, fin de un peldaño de Escalera) — NO después de cada micro-ejercicio individual dentro de Praxias (serían demasiados chequeos para ejercicios de 15-20s).
**Estrellas → Monedas (confirmado con el usuario, alcance "solo app interna")**: se sacó el contador de ⭐ del Mapa y de Mamá, quedó solo 🪙. El campo `estrellas` y su lógica de cálculo de nivel/onboarding NO se tocaron (calcularNivel ya usaba `totalEjerciciosHechos`, no estrellas). La narrativa de "primera estrella" del onboarding/landing queda intacta a propósito.
**Fix de monedas**: al principio solo sumaban en la PRIMERA vez que se completaba cada ejercicio (mismo gate que estrellas) — el usuario probó repitiendo un ejercicio ya hecho y no vio moneda nueva. Corregido: la moneda se gana SIEMPRE (cada intento cuenta), separado del gate de "primera vez" de las estrellas.
Próximo paso: falta que el usuario pruebe la sesión continua en su celular real — no se pudo verificar visualmente en este entorno.

⏸️ CHECKPOINT — Sesión 6/7. **Economía de monedas + tienda — HECHO y verificado con tsc.**
- `Progreso` nuevo: `monedas` (se suma 1 en cada `marcarPraxiaHecha`/`marcarSonidoHecho`/`marcarNivelEscaleraHecho`, mismo gate de "solo la primera vez" que ya usaban las estrellas) y `accesoriosComprados`.
- `lib/accesorios-data.ts`: `Accesorio` ahora admite dos modalidades — `imagen`+`lograda` (los 4 premios de logro, sin cambios) o `emoji`+`precio` (los nuevos de tienda). 6 accesorios nuevos sin arte a medida (moño/guantes/gorra/anteojos/collar/chaleco, 5-15 monedas) — usan emoji porque no hay forma de generar imágenes en este entorno; si el usuario quiere que se vean tan bien como los 4 originales, hay que generarlos con prompts igual que esos.
- `/app/premios` "Mi personaje": muestra saldo de monedas arriba, separa "Tus logros" (4 de imagen real) de "Tienda" (6 comprables), comprar equipa automático. `AccesorioOverlay` nuevo (helper) dibuja imagen O emoji según cuál tenga el accesorio.
- Mapa (`/app`): agregado un pill de monedas (con conteo animado) debajo de racha/estrellas, linkea a Premios.
⚠️ Pendiente del usuario: **NO se pudo verificar visualmente** que el tamaño/posición (top/left/tamaño) de los 6 accesorios de tienda calcen bien sobre el retrato del León — son valores estimados a ojo, igual que los 4 originales al principio.
Próximo paso EN CURSO (no terminado): sesión continua — que al terminar un ejercicio salte directo al siguiente ejercicio DE CUALQUIER SECCIÓN (Praxias→Sonidos→Escalera R→Escalera L) hasta cumplir los minutos de `planDiarioPorEdad`. Requiere: (1) nuevo `lib/sesion.ts` con la lista plana de ejercicios (mismo orden/desbloqueo que ya usa el Mapa, sin memorama), (2) 2 campos nuevos en Progreso (`segundosHoy`, `fechaSegundosHoy`) + `registrarTiempoPracticado()`/`segundosRestantesHoy()`, (3) que Praxias/Sonidos/Escalera lean un parámetro de URL (`?ex=id`) para poder abrir un ejercicio específico al llegar desde otra sección, y que al terminar el último de su propia sección busquen el siguiente paso GLOBAL en vez de solo volver a su propia lista. Es un cambio real de arquitectura de navegación — máximo cuidado de no romper el flujo manual existente (tocar una sección y elegir un ejercicio a mano debe seguir funcionando igual que hoy).

⏸️ CHECKPOINT — Sesión 6/7. El usuario generó y mandó las 4 imágenes de accesorios (prompts que le pasé antes). HECHO:
- Las imágenes llegaron como JPEG SIN transparencia real (el cuadriculado que se veía era textura dibujada, no alpha) — se les quitó el fondo por código (`lib/accesorios-data.ts` + script ad-hoc de remoción por umbral de gris/luminosidad, mismo enfoque que el logo). Corona/Capa/Sombrero quedaron perfectos. Gafas: el lente tenía un cuadriculado de COLOR (tintado naranja/turquesa, no gris puro) que no se pudo eliminar del todo sin arruinar la nitidez del marco — quedó una textura residual leve, aceptable al tamaño final pero no perfecta. Si en algún momento se ve mal, la solución de fondo sería pedir el lente sin cuadriculado en el prompt original.
- Guardadas en `superrre-app/public/accesorios/{sombrero,gafas,capa,corona}.png`, redimensionadas a 200-300px (los originales pesaban 1.5MB cada uno).
- `Accesorio` type ahora tiene `imagen` (ruta) + `ancho` (px) en vez de solo `emoji`+`tamaño`; `top`/`left` ahora son el CENTRO de la imagen (antes eran referencia para texto). Actualizado en 3 lugares: `/app/premios` (retrato + grid de selección) y `RevelacionPremio.tsx` (pantalla de cofre abierto).
Verificado con tsc (limpio), NO verificado visualmente en la app real (panel de preview sin funcionar toda la sesión) — pendiente que el usuario confirme que las posiciones (top/left/ancho estimados a ojo) calzan bien sobre el personaje real.
Próximo paso: 2 features nuevas pedidas por el usuario — (1) economía de monedas por ejercicio completado para comprar ropa, (2) ejercicio de completar palabras eligiendo la letra correcta entre R/L/D — se le presentó un plan corto antes de construir (regla de tareas grandes).

⏸️ CHECKPOINT — Sesión 6/7. **Usuario confirmó "ya quedó"** tras la ronda 4 (micrófono más sensible + cronómetro con voz en Praxias). Con esto se da por cerrado el pase de pulido de esta sesión iniciado con el skill `diseno` + la lista de 13 puntos.
Pendientes reales que quedaron abiertos, no perder de vista:
- Accesorios de "Mi personaje" (sombrero/gafas/capa/corona) siguen siendo emoji con sombra, no arte a medida — el usuario tiene 4 prompts de imagen listos para generar y mandar cuando quiera.
- Adaptación por edad: hecho tiempo+tolerancia del detector; el CONTENIDO (dificultad de palabras/oraciones) sigue sin adaptar, quedó explícitamente fuera de esta ronda.
- AppPorDentro.tsx (landing) sigue con tarjetas placeholder con ícono — pendiente reemplazar por screenshots reales de la app (bloqueado por el panel de preview que no funcionó en toda la sesión).
- Puntos 13 y 16 de la lista original del usuario (monedas para vestir al León comprándolas, ejercicio de completar frases eligiendo letras correctas) son FEATURES NUEVAS, no se tocaron — quedan en backlog.
Próximo paso exacto: preguntarle al usuario si sigue puliendo algo más o retoma la Secuencia Maestra (dominio propio / Auth real conectado al código / Hotmart / Resend).

⏸️ CHECKPOINT — Sesión 6/7. Ronda 4:
- Praxias: cronómetro ahora también se ESCUCHA (`hablar(String(segundos))` en cada tick). Deliberadamente NO se agregó en Sonidos/Escalera: ahí el micrófono está escuchando activamente durante el cronómetro (`estado==="escuchando"`), y la voz del propio celular podría hacer que el detector se auto-dispare pensando que fue el niño — riesgo real de falso positivo, no un descuido.
- `useRugidoDetector.ts`: usuario reportó que el rugido/gruñido SIGUE sin detectarse bien pese al fix de `resume()` de la ronda 1. Causa probable: el umbral estaba calibrado pensando en un grito abierto ("¡AAAH!"), pero un "grrrr" real (sonido gutural, poco flujo de aire) da un RMS mucho más bajo aunque el niño se esfuerce igual. Se subió el multiplicador de ganancia de 3.2 a 4.6 (equivale a bajar todos los umbrales por edad ~30% de una sola fuente, en vez de tocar cada valor de la tabla). ⚠️ Sigue sin verificación real del usuario — si esto tampoco alcanza, el siguiente paso sería instrumentar un log temporal del nivelVoz real que lee para calibrar con el dato real en vez de estimar a ciegas.
Verificado con tsc (limpio), NO verificado visualmente (panel de preview sigue sin funcionar toda la sesión).
Próximo paso exacto: esperar que el usuario pruebe de nuevo el rugido y el cronómetro con voz.

⏸️ CHECKPOINT — Sesión 6/7. Ronda 3, dos pedidos del usuario:
**Decisión técnica (DECIDE-INFORMA-AVANZA)**: adaptación real por edad de los ejercicios, con 2 mecanismos (criterio de fonoaudiología, no arbitrario):
- `factorTiempoPorEdad(edad)` en `lib/progress.ts`: multiplica la duración del cronómetro de Praxias/Sonidos/Escalera — 4 años=1.4x, 5=1.2x, 6=1x (base), 7=0.9x, 8+=0.8x. Los chicos tienen menos control motor/de aire, necesitan más segundos por intento.
- `ajustePorEdad(edad)` dentro de `hooks/useRugidoDetector.ts`: el hook ahora acepta `edad` opcional y ajusta umbral de volumen + tiempo sostenido — más permisivo para 4-5 años (umbral 24-28, sostener 150-175ms), más exigente para 7-8+ (umbral 34-36, sostener 220-240ms), 6 años = valores base (32/200, sin cambio).
Ambos threaded desde `p.edad` (useProgreso) en las 3 pantallas de práctica.
Pendiente de verdad adaptado y NO hecho todavía (quedó fuera de esta ronda, es más trabajo): dificultad/complejidad del CONTENIDO en sí (qué palabras/oraciones ve cada edad) — la Escalera Fonética sigue siendo la misma secuencia fija para todas las edades, solo cambió el tiempo y la tolerancia del micrófono.
**Otros fixes**: audio de instrucción en Praxias ahora se reproduce apenas se abre el ejercicio (antes del botón "Empezar" y la cuenta 3-2-1, quedó pendiente de un pedido anterior). 3 figuritas del Álbum repetían el mismo emoji que un ícono funcional ya visible en otra pantalla (Calentamiento=🐴 iba a Caballo, Casi Listos=🏍️ a Moto, Rugidos del León=🦁 al personaje) — reemplazadas por 🥉🥇🎤.
**Pendiente del usuario**: el sticker/emoji de los accesorios de vestir se sigue viendo "chafo" — no hay forma de generar imágenes en este entorno, se le pasaron 4 prompts (mismo estilo que el logo/mascotas) para que los genere con Gemini y los mande quede integrarlos.
Verificado con `tsc --noEmit` (limpio), NO verificado visualmente (panel de preview sigue sin compositar en toda la sesión).
Próximo paso exacto: esperar que el usuario pruebe esta ronda + que mande las 4 imágenes de accesorios si las genera.

⏸️ CHECKPOINT — Sesión 6/7. Ronda 2 de fixes tras probar el usuario en su celular real (Android + Chrome) el commit anterior (`3f15198`):
- Logo: la R seguía viéndose baja con el primer recorte — se ajustó de nuevo (menos margen arriba, que sobraba).
- Hero: agregado chip "y muchas palabras más" (se evitó poner un número específico tipo "30 palabras" porque no era exacto contando el contenido real de `escalera-data.ts` — solo hay 10 "palabra" formales por ahora).
- Agitacion.tsx: tarjetas "10-15 min diarios"/"En 6 meses, igual" tenían casi el mismo color que el fondo — pasadas a `bg-surface-primary` + sombra.
- `useRugidoDetector.ts`: usuario confirmó que YA detecta (el fix de `resume()` funcionó) pero sentía que tardaba — bajado `SOSTENER_MS` de 350 a 200.
- **Causa real de "videos estáticos" en Praxias encontrada**: `VideoCompanero` se usaba con `quieto={true}` en TODOS los ejercicios con pasos (los 9 de Praxias, ya que todos tienen `PASOS_EJERCICIOS`) — era comportamiento INTENCIONAL de una sesión anterior (congelar en un cuadro fijo), pero el usuario lo percibe como bug. Se quitó `quieto` para que el compañero loopee como el resto.
- Premios "Mi personaje": el accesorio (emoji) se veía "como un sticker" pegado sobre el video del personaje — se agregó `drop-shadow` para integrarlo mejor. Límite honesto: sigue siendo un emoji sobre un video real, no un asset ilustrado a medida — mejora parcial, no resuelve el fondo del problema (haría falta un accesorio dibujado/animado a medida).
Todo verificado con `tsc --noEmit` (limpio) y push a producción. NO verificado visualmente en este entorno — el panel de preview no compositó ni una sola vez en toda la sesión (Browser pane no se mostró). Toda la verificación de esta ronda depende de que el usuario la pruebe de nuevo en su celular.
Próximo paso exacto: esperar el nuevo feedback del usuario sobre esta ronda.

⏸️ CHECKPOINT — Sesión 6/7 SIGUE EN CURSO. Pase de "diseño premium" (skill `diseno`) + lista de 13 puntos que el usuario reportó tras probar la app en su celular real. **BUG CRÍTICO ENCONTRADO Y CORREGIDO**: `hooks/useRugidoDetector.ts` no llamaba `ctx.resume()` tras crear el `AudioContext` — en varios celulares (sobre todo iOS Safari) el contexto arranca "suspended" y el analizador solo lee silencio para siempre, aunque el mic tenga permiso. Esto explica el reporte del usuario ("rugí varias veces y no lo detectó"). Fix: `if (ctx.state === "suspended") await ctx.resume();` antes de conectar el analyser. ⚠️ Falta confirmación del usuario probando en su celular real — no se pudo verificar en este entorno (no hay micrófono real disponible para automatizar la prueba).
HECHO en este pase (código verificado con `tsc --noEmit`, NO verificado visualmente — el panel de preview no compositó frames en toda la sesión, pendiente capturas reales):
- Nav inferior: reemplazados los 5 emojis por íconos SVG propios (`IconMap/IconStairs/IconGiftBox/IconUser` nuevos en `icons.tsx`) con chip de fondo en el activo.
- `hooks/useConteo.ts` nuevo: conteo animado 0→valor (600ms, mismo easing que `fade-up`), aplicado a estrellas/racha (Mapa, Racha) y ejercicios (Mamá, Racha).
- Mamá elevada a dashboard: estrellas como dato héroe (36px), entrada escalonada por tarjeta, 2 niveles de profundidad de fondo.
- Landing: corregidos 2 textos de "¿Te suena familiar?" (frustración/llanto en vez de solo llanto; miedo a que crezca sin poder pronunciar/escribir, en vez de solo "entre a primaria"). Botón "Probar el Espejo gratis" de `Solucion.tsx` tenía estilo fantasma (borde tenue, sin relleno) casi invisible contra el fondo — pasado a sólido como el resto de los CTA.
- `MiniDemo.tsx` (demo del león en la landing): es una animación scripteada a propósito (no puede pedir permiso de mic antes del registro) — el copy decía "Demo del mecanismo real", lo cual engañaba; corregido a copy honesto ("Así se va a sentir").
- Logo (`app/icon.png` + `public/logo-mark.png`): recorte anterior dejaba la R descentrada hacia abajo, se cortaba en usos circulares — re-recortado desde el original (`C:\Users\doran\Downloads\Gemini_Generated_Image_ruf4ygruf4ygruf4.png`, crop x=214,y=37,580×580) con la R centrada y margen parejo.
- Onboarding: paso "Buena noticia" tenía al León chico (96px, se veía incompleto) → subido a 132px. Paso "Construyendo el mundo de..." tenía una tarjeta de mapa más grande que el León → recompuesto con el León protagonista (128px) + badge de libreta (`IconNotebook` nuevo) en vez de la tarjeta.
- Onboarding: agregada opción de edad "8 años o más" (antes tope en 7).
- `/app/premios`: Álbum ahora tiene barra de progreso + tarjetas logradas con `ring` dorado y sombra (antes borde plano); "Mi personaje" ahora tiene el anillo doble dorado+turquesa (el mismo dispositivo ownable del espejo de FICHA-ARTE.md) de fondo del retrato, y las tarjetas de accesorios con el mismo tratamiento coleccionable que las figuritas.
- `AppPorDentro.tsx` (landing, sección "La app por dentro"): decía "PRÓXIMAMENTE" y "la app está en construcción" sobre pantallas que YA existen — corregido el copy a "Ya disponible"/"cada pantalla ya está lista". PENDIENTE: reemplazar las tarjetas placeholder por screenshots reales de las 4 pantallas — no se pudo hacer porque el panel de preview no renderizó en toda la sesión.
PENDIENTE de este mismo pase (sin empezar): investigar por qué el usuario ve los videos de mascota "estáticos" en su celular — el código sí usa video real con chroma-key por canvas (`Mascota.tsx`); puede ser el mismo tipo de bug device-specific que el del micrófono. Falta que el usuario diga qué celular/navegador usó.
Plan gratis (punto 5 de su lista): el usuario preguntó 3 días vs sin límite — se le recomendó y confirmó DEJAR COMO ESTÁ (sin límite de días, limitado en contenido a Praxias+Espejo+1 sonido) — no se tocó código, no hacía falta.
Próximo paso exacto: reportar al usuario todo lo hecho, pedir su confirmación visual (screenshot/prueba en el celular real) ya que este entorno no pudo verificar nada visualmente, y retomar #38 (videos estáticos) con el dato de su celular/navegador.

⏸️ CHECKPOINT — Sesión 6/7. **PROTOCOLO DE PUBLICACIÓN (62) CERRADO — P0 a P5, P7 y P8 completos y verificados.** P2 Vercel: cuenta creada desde cero, proyecto `Supererre-app` importado desde GitHub (hubo que corregir "Directorio raíz" de `/` a `superrre-app` para que detectara Next.js), primer deploy exitoso. URL producción estable: `https://supererre-app.vercel.app` (OJO: la URL con hash random tipo `supererre-5oo1p085h-anni11.vercel.app` redirige al login de Vercel, no sirve para compartir — usar siempre el alias limpio de la sección Domains). P5+P8: 3 commits canarios de prueba (`bbcafc7`→0.1.1, `c454e7f`→0.1.2, `7a9164b`→revert a 0.1.0, este último dejó `package.json` en su estado original) — los 3 se publicaron solos, confirmados "Ready" por el usuario en Vercel → Deployments, sin pedir credenciales ninguna vez. Con esto queda demostrado: publicar Y revertir funcionan de punta a punta sin intervención manual en Vercel.
Certificado formal en [`PUBLICATION-CERTIFICATE.md`](PUBLICATION-CERTIFICATE.md) (raíz del repo) — veredicto: **APTO solo para el alcance certificado** (publicación continua GitHub→Vercel + esquema base Supabase), **NO APTO PARA VENDER TODAVÍA**: faltan P4 (variables de entorno — no aplica aún porque el código no consume Supabase, sigue en localStorage), P6 (dominio propio + Auth conectado al código), Hotmart y Resend.
Próximo paso exacto: preguntarle al usuario si sigue con dominio propio / conectar Auth real de Supabase al código / Hotmart / Resend, o si prefiere seguir puliendo la app primero.

⏸️ CHECKPOINT — Sesión 6/7. Arrancó la Sesión 7 (servicios externos) dentro de la misma sesión de trabajo, siguiendo el protocolo de `62-PUBLICACION-SEGURA-Y-CONTINUA.md`. Cuentas creadas desde cero (usuario no tenía ninguna): GitHub (`emperatrizmagneticamipersonali-coder`), Supabase (proyecto `supererre`, project-ref `tpjvjlzrigxnvluhpzep`), Vercel (`Supererre-app`, ver checkpoint más reciente arriba).
**P3 Supabase — COMPLETO Y VERIFICADO**: usuario corrió el SQL a mano en el SQL Editor (sin darme ningún token de cuenta completa, decisión correcta de seguridad). Tablas `parents`/`children`/`exercise_progress`/`purchases` con columnas correctas + 6 políticas RLS verificadas por CSV (`children_all_own` ALL, `exercise_progress_all_own` ALL, `parents_insert_own`/`parents_select_own`/`parents_update_own`, `purchases_select_own` — sin insert/update en purchases, correcto, solo el webhook debe escribir ahí). ⚠️ Igual que Praxias: la app en código SIGUE usando localStorage nada más — las tablas existen pero el código todavía no las consume (eso es trabajo aparte, no automático).
**P1 GitHub — COMPLETO Y VERIFICADO**: push exitoso a `emperatrizmagneticamipersonali-coder/supererre-app` (repo privado), SHA local y remoto coinciden (`6b37445...`). Costó mucho llegar: `git credential-manager` no puede abrir navegador en este entorno (no hay TTY real) — mismo problema con `supabase login` ("Cannot use automatic login flow inside non-TTY environments"). Solución que funcionó: Personal Access Token de GitHub (fine-grained, permiso Contents=Read/write, repo-scoped) guardado por el usuario directo en el Administrador de Credenciales de Windows (`git:https://github.com`) — nunca pasó por el chat.
⚠️ **NOTA PARA EL FUTURO — 2 trampas de GitHub en español que costó horas diagnosticar**: (1) el permiso "Contents" del token de grano fino se muestra en la UI en español como **"índice"**, NO "Contenido" — buscarlo así desde el principio. (2) Al agregar un permiso en esa UI, por defecto puede quedar en "solo lectura" — hay que verificar explícitamente que diga "Lectura y escritura" (Read and write), si no el push da 403 Permission denied aunque el repo esté bien seleccionado.
Próximo paso exacto: P2 (conectar Vercel al repo de GitHub para auto-deploy) — todavía no se creó la cuenta de Vercel.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. BUGS REALES CORREGIDOS en el sistema de premios (reportados por el usuario tras probarlo): (1) a veces "Reclamar mi premio" no mostraba nada, y (2) a veces aparecían varias estampas de golpe en un ejercicio que no era el último. Causa raíz: `RevelacionPremio` ESCANEABA todo el progreso buscando "cualquier figurita/accesorio no visto" — si había un backlog de fases completadas sin visitar la lista, mostraba varias encadenadas, y el timing del scan podía fallar. Rediseño: `RevelacionPremio` ahora recibe el id EXACTO del premio recién ganado (calculado en el momento del clic en "Reclamar mi premio" — `figuritaDeSeccion()` en Praxias, `!onSiguiente` en Sonidos, `escalera-${silaba}` en Escalera), sin escanear nada — 100% determinístico, un solo premio por vez. Cuando terminar una sección TAMBIÉN completa una sección mayor (ej. Elevador termina Praxias entera → figurita Y accesorio "sombrero" a la vez), se resuelve con una cola de máximo 2: primero la figurita, al cerrarla recién aparece el accesorio — nunca simultáneos.
NUEVO: al abrir el cofre, además del premio grande se muestra una tira con las 14 figuritas del álbum — a color las que ya tiene (incluida la nueva, resaltada), en blanco y negro/opacas las que le faltan, con el texto "Colecciona las demás figuritas".
Verificado en navegador real: Praxias (Elevador → figurita 🏍️ + tira de 14 con 3 en color → cerrar → accesorio "Sombrero de Explorador" aparece DESPUÉS, no junto) y Sonidos (Gruñido Bajito, último sonido de León → mensaje automático → Reclamar → cofre). Build de producción limpio (17/17).
Próximo paso exacto: avisar al usuario que los 2 bugs que reportó ya están corregidos, y que revise de nuevo.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Ajuste al sistema de premios: el usuario pidió que el aviso de "sección completa" aparezca AUTOMÁTICAMENTE al terminar el último ejercicio de la sección (ej. Elevador en Praxias), no recién al volver a la lista. Se agregó a las 3 pantallas de práctica (Praxias, Sonidos, Escalera) la detección de "es el último ejercicio de esta sección" (`esUltimoDeSeccion` en Praxias por nivel; `!onSiguiente` en Sonidos; `nivel.id.endsWith("-2")` en Escalera, ya que cada peldaño siempre tiene 3 niveles) — en ese momento, en vez del mensaje normal de "¡Bien hecho!", se muestra "🎉 ¡Terminaste esta sección de ejercicios! Te ganaste un premio." y el botón cambia a "Reclamar mi premio", que lleva directo a la lista donde `RevelacionPremio` ya está esperando y abre el cofre al toque.
Verificado en navegador real: se completó "El Elevador" (último de "Casi Listos para la R") y apareció el mensaje + botón en el momento exacto, sin tener que volver primero. Build de producción limpio (17/17).
Próximo paso exacto: esperar la revisión del usuario (dijo que iba a revisar las imágenes/capturas él mismo).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Sistema de premios por fase completada (el usuario pidió "un premio cada vez que termine una fase" y eligió, de 4 opciones que le di, combinar dos: álbum de figuritas + vestir al personaje).
HECHO:
- `lib/figuritas-data.ts` — 14 figuritas (3 Praxias por nivel, 5 Escalera R por peldaño, 5 Escalera L por peldaño, 1 Sonidos León completo), cada una con el emoji del propio ejercicio/palabra que el niño ya aprendió (no genérico).
- `lib/accesorios-data.ts` — 4 accesorios "de vestir" (sombrero=Praxias completo, gafas=Sonidos León completo, capa=Escalera R completa, corona=Escalera L completa) — premio grande y esporádico, a diferencia de las figuritas que son chicas y frecuentes.
- `lib/progress.ts` — nuevos campos persistidos: `figuritasVistas`, `accesoriosVistos` (para no repetir la revelación) y `accesorioEquipado`.
- `components/app/RevelacionPremio.tsx` — cofre 🎁 que aparece solo cuando hay un premio nuevo sin ver; se coloca en las 3 pantallas de lista de fase (Praxias, Sonidos, Escalera). Muestra las figuritas ANTES que los accesorios, una por una (cola), y marca cada una como vista al cerrar.
- `/app/premios` ahora tiene 3 pestañas: Memorama (lo que ya había), Álbum (grilla de las 14 figuritas, bloqueada=candado) y Mi personaje (retrato del León/Pirata con el accesorio equipado superpuesto + selector de accesorios desbloqueados).
BUG REAL ENCONTRADO Y CORREGIDO durante la verificación: la animación `animate-bubble-pop` (fade-in con opacity 0→1) se quedaba TRABADA en el cuadro inicial (opacity:0, por eso el premio revelado no se veía nunca, aunque el dato sí se guardaba bien) — se sacó esa clase del contenido revelado para no depender de que la animación corra; ahora se ve siempre, animado o no. Nota aparte: varias veces el screenshot del navegador de esta sesión mostró contenido viejo/vacío que en el DOM real ya estaba actualizado y correcto — es un retraso del propio navegador de pruebas, no un bug de la app (se confirmó comparando contra el DOM/localStorage reales cada vez).
Verificado en navegador real, de punta a punta: se completó "Calentamiento" → apareció el cofre → se abrió → mostró la figurita 🐴 correcta → se cerró → quedó marcada en el Álbum (1/14). Se completaron los 9 ejercicios de Praxias → aparecieron las 3 figuritas en cola → después el accesorio "Sombrero de Explorador" 🤠 → se probó equiparlo en "Mi personaje" y se ve superpuesto sobre el León correctamente.
Próximo paso exacto: confirmar que el build de producción final terminó limpio, y avisarle al usuario que también esto está listo para revisar.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Lote grande de pulido pedido de una sola vez por el usuario, los 8 puntos:
1. **Diagramas de pasos en los 8 ejercicios que faltaban** (Ola, Reloj, Pintor, Columpio, Serpiente, Moto, Ardilla, Elevador) — se generalizó el patrón de `PasosCaballo.tsx` a un motor reutilizable: `components/app/PasosEjercicio.tsx` (SVG boca+dientes+lengua parametrizable: posición X/Y, escalas, meta a resaltar, dientes de abajo opcionales, efectos clop/vibra/rápido, referencia nariz/mentón) + `lib/praxias-pasos.ts` (config de 2-3 pasos por ejercicio, incluye Caballo migrado al mismo sistema). `PasosCaballo.tsx` se borró (reemplazado). Verificado visualmente Ola, Elevador y Columpio (este último necesitó un ajuste: la lengua no llegaba a sobresalir de los labios hacia la nariz, se aumentó el desplazamiento).
2. **Compresión + recorte de los 13 videos** (9 Praxias + 4 Sonidos León): se instaló `ffmpeg-static` vía npm (`--no-save`, no queda en package.json — es una herramienta de esta sesión, no una dependencia del proyecto) tras confirmar que SÍ trae un binario funcional (libx264 incluido). Cada video se recortó (se sacan los primeros 3s "muertos" antes de que empiece el gesto — el usuario se había quejado de que el video recién arrancaba a mostrar la técnica cuando ya se acababa el cronómetro) y se re-codificó a 480px de ancho + crf 28 + sin audio. Resultado: de ~2-2.6MB por video a 41KB-410KB (~90-98% menos peso), calidad visual verificada intacta con un frame de muestra. El cuadro fijo "quieto" de `VideoCompanero.tsx` (usado para el compañero chico bajo los pasos) tenía un timestamp fijo (4.5s) calibrado para los videos VIEJOS sin recortar — se corrigió a 1.5s para que siga apuntando al mismo instante real tras el recorte.
3. **Sonidos: auto-avance + cronómetro** — al terminar de detectar un sonido, el botón ahora dice "Siguiente" y pasa directo al próximo sonido del modo actual (sin volver a la lista), igual que ya hacía Praxias; en el último sonido dice "¡Terminaste! Volver". Se agregó un cronómetro visual (⏱ 8s por defecto) mientras escucha, mismo estilo que Praxias.
4. **Escalera: cronómetro visual** agregado a `EscaleraLetra.tsx` (⏱ 10s por defecto mientras escucha) — sin auto-avance (el usuario solo pidió el cronómetro ahí, el auto-avance lo pidió explícitamente solo para Sonidos).
5. **Plan diario por edad**: `lib/progress.ts` ahora persiste `edad` (recogida en el onboarding, antes se descartaba) y expone `planDiarioPorEdad()` — decidido con criterio de fonoaudiología (sesiones cortas y diarias, no una sola larga): 4 años=5min, 5 años=6min, 6 años=8min, 7 años=10min. Se muestra en tarjeta nueva en `/app/mama`. `edad` viaja por la URL de onboarding→login (`irALogin` en `OnboardingFlow.tsx` → `LoginForm.tsx` → `iniciarProgreso`).
6. **Se quitó la opción de elegir Pirata en el onboarding** (paso "interes" completo, con su pantalla `StepInteres`) — ahora `interes` es una constante fija en `"leon"`, el funnel salta directo de "dolor" a "reconocimiento". NO se borró nada de Pirata (assets, videos, ramas `tema==="pirata"` en todos los componentes) — queda listo para reactivar más adelante reponiendo el paso. `StepInteres` sigue definida pero sin usarse (código pausado, no muerto, a pedido explícito del usuario).
7. **Colores del Mapa de Islas invertidos**: el camino/sendero recorrido ahora es dorado (`--brand-primary`, antes turquesa) y TODOS los nodos/círculos de sección ahora son azules (`--brand-secondary`, antes variaban primary/secondary/accent por sección) — a pedido explícito del usuario ("que el dorado sea el del camino y azul el de la rueda"). Se quitó el sistema `Tono`/`TONO_VAR`/`TONO_SOLIDO` por sección en `app/app/page.tsx` (quedó sin uso tras el cambio).
8. Limpieza: se borró `public/mascots/_review-referencia.mp4` (archivo suelto de una captura de referencia de otra sesión, no estaba enlazado en ningún lado).
Verificado hasta ahora: tsc limpio en cada paso, capturas reales de Ola/Elevador/Columpio (pasos), Mapa (colores), Mamá (plan por edad), flujo completo de Sonidos con auto-avance (Rugido→Ronroneo). Falta: build de producción corriendo en background al momento de escribir este checkpoint, y un barrido final de capturas antes de avisarle al usuario que puede revisar todo (así lo pidió: "cuando termines enviame un mensaje para revisar todo").
Próximo paso exacto: confirmar que `npm run build` terminó limpio, y mandar el mensaje de cierre pedido.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. El usuario, tras evaluar los videos de IA de los personajes, pidió que los ejercicios de Praxias muestren la técnica EXACTA (como apps de fitness que muestran el movimiento real, no una aproximación linda). Se probaron 2 enfoques que el usuario RECHAZÓ antes de llegar al bueno:
(1) Diagrama animado de perfil (corte lateral de una cabeza con boca/dientes/lengua, animación continua en loop) — el usuario dijo que sentía que un niño no iba a entender el movimiento así.
(2) Se le preguntó directamente con 3 opciones (video real filmado / pasos numerados simples / volver solo al video del León) — ELIGIÓ "pasos numerados simples".
LO QUE QUEDÓ (aprobado): `components/app/PasosCaballo.tsx` — 3 cuadros FIJOS (sin animar) en fila, cada uno con número (1/2/3), una boca simple de frente (labios+dientes en arco+lengua) y un texto corto: "1. Lengua arriba" (lengua ya subiendo), "2. Detrás de los dientes" (lengua arriba tocando el puntito dorado que marca la meta), "3. ¡Suelta fuerte!" (lengua abajo + ráfaga de líneas). Es dibujo por código (SVG), no depende de ningún video generado, así que la técnica siempre es 100% exacta. Debajo de los 3 pasos se dejó el video del León pequeño (`VideoCompanero`, componente nuevo que unifica el bloque de video+canvas+caption reutilizado por los otros 8 ejercicios) con el texto "El León también lo intenta contigo" — el usuario pidió explícitamente combinar ambos: la técnica exacta arriba, el personaje acompañando abajo.
BUG REAL ENCONTRADO POR EL USUARIO Y CORREGIDO: el paso 1 decía "Lengua arriba" pero el dibujo mostraba la lengua ABAJO (mismo valor que el paso 3 por error de copy-paste al escribir `lenguaY`). Corregido en `PasosCaballo.tsx`: paso 1 y 2 ahora suben la lengua (44 y 30 respectivamente), solo el paso 3 la deja abajo. Verificado visualmente en pantalla real tras el fix.
Se borró el enfoque descartado (`DiagramaCaballo.tsx` y sus keyframes `caballo-lengua/cresta/clop` en `globals.css`) para no dejar código muerto. Quedan 2 tokens de color nuevos y sí usados: `--diagram-labio` y `--diagram-cavidad` en `globals.css` (derivados de `--brand-accent` vía `color-mix`, no hex suelto — el linter de diseño del repo lo exige).
Referencia visual que el usuario compartió y SÍ aprobó como dirección correcta: un video de una app de fitness (WhatsApp Video) mostrando una lista de ejercicios con iconitos planos simples (no video real, no IA) — confirmó que el camino de "dibujo simple, no animación generada" era el correcto, solo faltaba ajustar a pasos fijos en vez de loop continuo.
Build de producción y tsc verificados limpios ANTES del fix del bug de paso 1 (el fix es un cambio trivial de un valor numérico, bajo riesgo, pero falta correr un build final después del fix si se quiere el检 100% formal).
Próximo paso exacto: el usuario todavía no confirmó si este patrón de "3 pasos fijos" le gusta para pasar a construirlo en los otros 8 ejercicios (cada uno con su propia secuencia de 2-3 pasos según su instrucción), o si prefiere revisar antes cómo se ve/siente en su propio celular. Preguntar antes de replicar a los 8 restantes.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. LOS 9 EJERCICIOS DE PRAXIAS YA TIENEN VIDEO REAL (caballo, ola, reloj, pintor, columpio, serpiente, moto, ardilla, elevador) — lote completo, ya no queda ninguno con el fallback mascota+emoji genérico (versión León; Pirata sigue pendiente).
BUG REAL ENCONTRADO Y CORREGIDO: el usuario reportó que Moto/Ardilla/Elevador mostraban al León "solo ladeando" (el fallback genérico) pese a haber compartido esos 3 videos antes — se confirmó que los archivos SÍ estaban copiados en `public/mascots/` pero nunca se había agregado el campo `videoDemo` de esos 3 ejercicios en `lib/praxias-data.ts` (quedó pendiente de una sesión anterior, no fue detectado hasta que el usuario lo notó en uso real). Se agregaron los 3 campos faltantes. Verificado en navegador: los 3 ejercicios ahora muestran su video real.
SEGUNDO AJUSTE del mismo reporte: el usuario notó que los videos "tardan en aparecer". Causa: sin `preload`, el navegador no siempre prioriza la descarga del video hasta que hace falta reproducirlo, y mientras carga el canvas se queda en blanco (parece congelado). Se agregó `preload="auto"` al `<video>` y un estado `listo` en `DemoEjercicio` (`app/app/praxias/page.tsx`) que muestra un círculo con pulso suave (skeleton) mientras el video no está listo (`onCanPlay`), con fade-in del canvas real cuando termina — así la espera se ve intencional, no rota. NO se pudo resolver la causa de fondo (los videos pesan ~2.5MB cada uno, hay 9 en total, y no hay `ffmpeg` disponible en este entorno para comprimirlos) — sigue siendo una espera real en redes lentas, solo se mejoró cómo se percibe. Si el usuario quiere videos más livianos de verdad, hay que comprimirlos ANTES de compartirlos (herramienta de compresión externa) o pedirle a la IA de video que exporte en menor resolución/duración.
Próximo paso exacto: preguntar al usuario si esta mejora de carga se siente suficiente en su celular real, y si quiere las versiones Pirata de los 9 ejercicios (con los mismos prompts ya traducidos, cambiando el personaje).

⏸️ CHECKPOINT — Sesión 6 (histórico). BUG REAL CORREGIDO: "El Reloj" y "El Pintor" tenían los videos CRUZADOS (el usuario había descrito verbalmente cuál era cuál y yo los asigné mal). El usuario lo detectó renombrando sus 4 archivos originales con el nombre correcto de cada ejercicio y pidiendo que verificara; se comparó por hash MD5 contra los archivos ya integrados en `public/mascots/` y se confirmó el cruce. Se corrigió intercambiando el CONTENIDO de `praxia-reloj.mp4` y `praxia-pintor.mp4` (sin tocar `lib/praxias-data.ts`, que ya apuntaba a los nombres correctos). Verificado visualmente en la app real: "El Reloj" ahora muestra el cachorro con la lengua afuera (coherente con tocar las esquinas de la boca) y "El Pintor" muestra el león adulto rugiendo con la boca bien abierta (coherente con necesitar ver el techo de la boca). Caballo y Ola ya estaban correctos, no se tocaron.
Videos reales de demostración en Praxias: 4 de 9 ejercicios ya tienen video (caballo, ola, reloj, pintor), los otros 5 (columpio, serpiente, moto, ardilla, elevador) siguen con el fallback mascota+emoji hasta que el usuario los genere.
Cambio de estructura: `videoDemo` en `lib/praxias-data.ts` pasó de un string único a `{ leon?: string; pirata?: string }` — cada ejercicio puede tener un video distinto por personaje (`DemoEjercicio` en `app/app/praxias/page.tsx` elige `ex.videoDemo?.[tema]`). Hoy solo hay versiones León; ninguna versión Pirata todavía (el usuario aún no compartió ninguna).
El video de "El Pintor" (`praxia-pintor.mp4`) tenía un fondo con degradado/viñeta (~176-224 RGB, no uniforme como los demás) que dejaba manchas grises opacas en las esquinas con el umbral viejo — se bajó el umbral global de `useVideoChromaKey.ts` de `min>185` a `min>165` (feather hasta 195). Verificado que NO rompió los videos ya integrados (caballo/ola/reloj siguen limpios, sin partes del personaje erosionadas) — queda un rastro muy tenue (~15% opacidad) en 2 píxeles de borde del Pintor, imperceptible en uso normal.
⚠️ Aviso pendiente de resolver con el usuario (no bloqueante, ya se le avisó una vez): el video de "El Reloj" usa un león ADULTO rugiendo, distinto al cachorro que aparece en caballo/ola/pintor y en el resto de la app — inconsistencia visual de personaje que el usuario conoce y por ahora aceptó tácitamente (confirmó el ejercicio sin pedir cambiarlo). Si en algún momento se ve raro en conjunto, se puede regenerar con el prompt del cachorro.
Próximo paso exacto: build de producción corriendo en background para verificación final de este lote; luego seguir recibiendo videos para columpio/serpiente/moto/ardilla/elevador (prompts en español ya entregados al usuario), y eventualmente las versiones Pirata de todos.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. El usuario probó el fondo blanco de los videos de mascota en su propia pantalla y confirmó que el truco de CSS (`mix-blend-mode: multiply`, ver checkpoint anterior) NO funcionó del lado del usuario, aunque en mi navegador de pruebas sí se veía bien — es un truco conocido por ser poco confiable con `<video>` entre navegadores/celulares reales (safari/mobile a veces desvían el video a una capa de composición que ignora blend-mode). Se reemplazó por una solución real y sólida: `hooks/useVideoChromaKey.ts` — un hook que dibuja el `<video>` oculto dentro de un `<canvas>` cuadro a cuadro (`requestAnimationFrame`) y vuelve TRANSPARENTE DE VERDAD (alpha=0, no un truco visual) cualquier píxel casi blanco/gris claro (umbral: min(r,g,b)>205 y diferencia entre canales <25, con una zona de transición suave para no dejar bordes dentados). Esto es matting real por código, funciona igual en cualquier navegador porque solo usa Canvas 2D (soportado universalmente), no depende de compositing de GPU para `<video>`.
Se actualizó `components/app/Mascota.tsx` (usado en Mapa/Praxias/Premios/onboarding) para usar el hook con ajuste "contain", y se QUITÓ la tarjeta blanca redondeada que envolvía al personaje (ya no hace falta disimular ningún cuadrado — ahora el personaje flota de verdad sobre el fondo de la app, con una sombra sutil tipo `drop-shadow` para dar profundidad). Se actualizó el círculo "espejo mágico" de la Primera Victoria en `OnboardingFlow.tsx` (`StepVictoria`) para usar el mismo hook con ajuste "cover" (llena el círculo recortando en vez de dejar franjas).
Verificado con capturas reales (no solo lógica): en el Mapa de Islas, el león y el pirata ahora flotan sin ningún rectángulo ni cuadro blanco visible, en los dos temas. En el espejo circular dorado del onboarding, el pirata se ve recortado limpio dentro del círculo, sin ninguna franja blanca y sin el tinte dorado turbio que daba el truco de CSS anterior. Sin errores de consola, tsc limpio.
Próximo paso exacto: el usuario pidió "ver la app completa" — se abrió el panel de vista previa en `/` para que la recorra él mismo. Esperar su feedback tras ver esta corrección de los videos, y luego preguntar si seguimos puliendo o pasamos a la Sesión 7 (servicios externos: GitHub, Supabase, Vercel, Resend, dominio, Hotmart).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Tres pedidos en este tramo, los 3 resueltos:
(1) PREGUNTA DEL USUARIO — respondida, no es código: "¿va a reconocer la pronunciación cuando esté al público?". Respuesta honesta dada: NO reconoce si dice la R correctamente — por diseño desde el inicio del proyecto (ver [[decisiones-tecnicas]] previas), `useRugidoDetector` solo mide VOLUMEN/sonido local (nunca sube audio a internet), no hace reconocimiento de habla real. Además se identificó la causa técnica probable de lo que el usuario vio (estrella sin hablar): `hooks/useRugidoDetector.ts` cae a `sin-microfono` en CUALQUIER falla de `getUserMedia` (permiso denegado, o — la más probable en su caso — abrir la app por una IP de red local en HTTP en vez de `localhost`/HTTPS, que los navegadores bloquean en silencio) y el modo `sin-microfono` YA cuenta como "logrado" (decisión de sesión anterior para no trabar a un niño con mic roto). Esto es coherente con la promesa de privacidad del negocio pero es un límite real de producto que el usuario debe conocer antes de vender — no se tocó código, es una decisión de producto ya tomada, solo se le explicó con claridad.
(2) VIDEOS DE LOS PERSONAJES: el usuario compartió 2 videos (`Cartoon_pirate_boy_waving_202608121921.mp4`, `Lion_cub_waving_paw_202608121904.mp4`) para reemplazar las fotos estáticas de León/Pirata. Se copiaron a `superrre-app/public/mascots/leon.mp4` y `pirata.mp4`. `components/app/Mascota.tsx` pasó de `<Image>` a `<video autoPlay loop muted playsInline>`, reutilizado automáticamente en TODOS los lugares que ya usaban `Mascota` (Mapa de Islas, Praxias, Premios) sin tocar esos archivos.
FONDO BLANCO DE LOS VIDEOS: el usuario pidió después (mid-turn) quitarle el fondo blanco a los videos. A diferencia del logo estático (sesión anterior, el flood-fill falló), aquí SÍ funcionó una solución real: se verificó por muestreo de píxeles vía canvas en el navegador que ambos videos tienen fondo CASI BLANCO UNIFORME (~245-254 RGB, no degradado) — eso permite el truco de CSS `mix-blend-mode: multiply` en el `<video>`: contra un fondo blanco/crema (multiply con blanco = sin efecto → el blanco del video "desaparece"), y en el círculo dorado del espejo de la Primera Victoria del onboarding, el multiply tiñe el fondo del video de dorado, fundiéndose con el círculo. Verificado con captura real: se ve el personaje limpio, sin ningún cuadrado blanco visible, en ambas tarjetas y en el espejo circular. No hace falta ffmpeg ni IA de recorte — fue la elección de técnica correcta para ESTE caso (fondo uniforme), a diferencia del logo (fondo degradado, esa técnica no aplica ahí).
(3) PERSONALIZACIÓN DEL ONBOARDING TRAS LA PREGUNTA LEÓN/PIRATA: bug real confirmado — después de `StepInteres` (la pregunta), los pasos siguientes (`StepReconocimiento`, `StepLoading`, `StepRevelacion`, `StepVictoria`) IGNORABAN la respuesta y mostraban León/🦁 fijo sin importar qué eligiera el niño. Se corrigió threading la prop `interes` a los 4 componentes, con copy y video personalizados en cada uno: StepReconocimiento ("Mateo y su Pirata guía" + video), StepLoading ("Cargando el Espejo del Pirata" + mini-video flotante), StepRevelacion (video grande del personaje), StepVictoria (todo el copy: "Toca al Pirata y grita con él: ¡ARRR!" / "el Pirata te escuchó gritar" vs. el equivalente León/GRRR/rugir — antes decía "León"/"GRRR" siempre). También se corrigió un bullet duplicado/contradictorio en `StepPaywall` que decía "El Espejo del León" fijo aunque el bullet de abajo ya dijera "Modo Pirata desbloqueado" (mismo paywall, dos menciones contradictorias).
Verificado en navegador paso a paso (con clics reales, separando las llamadas para evitar falsos negativos de timing): seleccioné "piratas", avancé por Reconocimiento → Loading → Revelación → Victoria, y CADA pantalla mostró contenido de pirata real (video correcto, copy correcto) — captura final del espejo circular de la Primera Victoria confirma visualmente el personaje pirata sin fondo blanco, con el copy "Toca al Pirata y grita con él: ¡ARRR!". Sin errores de consola, sin desborde a 375px, tsc limpio.
Nota metodológica de esta sesión: al probar el onboarding con scripts que esperan (`await wait(ms)`) DENTRO de una sola llamada a herramienta, los tiempos de espera cortos (600ms) a veces no reflejaban el estado real — dividir la prueba en llamadas SEPARADAS (clic en una llamada, verificar en la siguiente) fue más confiable. No es un bug de la app, es una particularidad del entorno de automatización.
Próximo paso exacto: preguntar al usuario si hay más pulido, o si pasamos a la Sesión 7 (servicios externos: GitHub, Supabase, Vercel, Resend, dominio, Hotmart). También sigue pendiente que el usuario confirme si quiere que le explique con más detalle las opciones para tener reconocimiento REAL de pronunciación más adelante (cloud speech API paga, rompe la promesa de privacidad 100% local — sería una decisión de negocio grande, no técnica).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Último pedido: colocar el isotipo/logo real del usuario (escalera de monedas doradas formando una R + destello, con "SUPER ERRE" debajo, generado con Gemini) como logo profesional de la app — HECHO, con una decisión técnica importante documentada abajo.
Archivo original: `C:\Users\doran\Downloads\Gemini_Generated_Image_ruf4ygruf4ygruf4.png` (1024x1024, con fondo degradado naranja).
INTENTO DE QUITAR EL FONDO (como pidió el usuario): se probó un removedor de fondo por flood-fill (relleno por inundación con tolerancia local, sin depender de ninguna IA externa — no hay ninguna disponible en esta sesión) hecho a medida en C#/.NET vía PowerShell. RESULTADO: falló de forma visible — las monedas superiores del logo usan tonos dorados MUY claros, casi del mismo color que el fondo degradado claro, así que el algoritmo "comió" agujeros blancos dentro de las monedas y la cara de la R en vez de solo quitar el fondo. Se descartó ese resultado (no se usó, se ve roto). CONCLUSIÓN técnica real, no una limitación inventada: este logo específico no es un buen candidato para un recorte automático — haría falta una IA de recorte de verdad (tipo remove.bg) que no está disponible aquí.
DECISIÓN (sin bloquear al usuario por esto): en vez de forzar una transparencia rota, se recortó el isotipo (solo el símbolo: monedas+R+destello, SIN el texto "SUPER ERRE") a un cuadrado limpio de 500×500 CON su propio fondo degradado naranja intacto — igual que hacen íconos de apps reales (iOS, Slack, etc., que son cuadrados con fondo de color, no transparentes). Coordenadas de recorte encontradas por muestreo de píxeles (no a ojo): x=235,y=110,ancho=500,alto=500 del original.
Se colocó en: `superrre-app/app/icon.png` (favicon — Next.js lo detecta solo; se borró un `favicon.ico` viejo del scaffold inicial que estaba tapando el nuevo) y `superrre-app/public/logo-mark.png` (para usarlo en componentes). Se reemplazó el emoji 🦁 placeholder por la imagen real del logo en 3 lugares — los únicos que eran REALMENTE el logotipo de marca (se dejaron sin tocar todos los demás 🦁 del resto de la app, que son el personaje/mascota León, no el logo): `components/app/Header.tsx` (nav de la landing), `components/app/Footer.tsx`, `components/app/LoginForm.tsx` (encabezado de /login).
Verificado: tsc limpio, sin errores de consola, sin desborde a 375px, confirmé por fetch que `/logo-mark.png` y `/icon.png` responden 200 con `image/png`, y que el `<link rel="icon">` real del HTML ahora apunta al nuevo `icon.png` (antes apuntaba al favicon.ico viejo). El panel de vista previa volvió a fallar (no pude verlo con mis propios ojos), así que toda esta verificación fue por código/red, no visual.
Pendiente del usuario: confirmar que el logo se ve bien en su pantalla (pedirle que refresque fuerte / limpie caché para ver el favicon nuevo, los navegadores cachean favicons agresivamente). Si más adelante quiere una versión con fondo transparente de verdad (por ejemplo para superponerlo sobre el fondo crema de la app en otros lugares), va a necesitar generarla con una herramienta de recorte real (remove.bg o pedirle a la misma IA que generó el logo una versión con fondo transparente) y compartirla — ahí sí se puede integrar directo.
Próximo paso exacto: preguntar al usuario si el logo se ve bien, si hay más pulido, o si pasamos a la Sesión 7 (servicios externos: GitHub, Supabase, Vercel, Resend, dominio, Hotmart).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Último pedido: animación de celebración premium al completar cada lección, usando un archivo Lottie que el usuario compartió (`C:\Users\doran\Downloads\celebrate.json`, ~64KB, animación válida de ~5.3s) — HECHO. Se instaló `lottie-react` (única librería nueva de la sesión, vía npm), se copió el archivo a `superrre-app/public/lottie/celebrate.json` (se sirve como asset estático, NO se empaqueta en el JS — se carga con `fetch` la primera vez y se cachea en memoria para las siguientes veces, así no pesa en el bundle inicial). Se creó `components/app/Celebracion.tsx`: overlay de pantalla completa con fondo oscuro semitransparente (`var(--surface-overlay)`), la animación centrada + un mensaje corto ("¡Excelente!" por defecto, personalizable), se cierra sola cuando termina la animación O al tocar la pantalla (para no bloquear a un niño impaciente — mismo criterio que el cronómetro de Praxias).
Integrado en las 4 pantallas donde se completa una lección: Praxias (`Ya lo intenté`), Sonidos (detectado o sin-micrófono), Escalera R y L (mismo detector), y Memorama al ganar (mensaje distinto: "¡Lo lograste!"). Cada integración es una sola línea `<Celebracion activa={...} />` reutilizando el mismo componente.
Verificado a fondo (el panel de vista previa volvió a fallar intermitentemente esta vez, así que la verificación fue por DOM/consola en vez de visual): confirmé que el overlay se monta de verdad con el SVG de la animación (256 elementos de forma reales, no un placeholder vacío) tanto en Praxias como en el Memorama, con el mensaje correcto en cada caso, que se cierra solo, y CERO errores de consola en todo el flujo. tsc limpio.
Próximo paso exacto: preguntar al usuario si la animación se ve y se siente como esperaba (no pude verla yo mismo por el problema del panel), y si hay más pulido pendiente o pasamos a la Sesión 7 (servicios externos: GitHub, Supabase, Vercel, Resend, dominio, Hotmart).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO. Último pedido: que la Escalera se vea "como el mapa, con estrellas cada paso hasta desbloquear el siguiente nivel" — HECHO. `components/app/EscaleraLetra.tsx` se rediseñó de punta a punta: antes era una navegación de 2 pisos (lista de 5 peldaños → tarjetas de 3 pasos dentro de cada uno); ahora es el MISMO estilo de sendero que el Mapa de Islas — cada peldaño (RA/RE/RI/RO/RU) muestra su propio mini-camino de 3 nodos circulares conectados por una curva, colores sólidos rotando primary/secondary/accent por peldaño (mismo `TONO_SOLIDO`/`color-mix` 3D del Mapa), estrella dorada rellena sobre los pasos completados, candado en los bloqueados, y la mascota León/Pirata flotando animada sobre el paso actual — igual que el Mapa. Se eliminó el estado intermedio `grupoActivo`: tocar un nodo abierto va DIRECTO a la pantalla de práctica (antes había que entrar a la tarjeta del peldaño primero). El desbloqueo secuencial (paso a paso, peldaño a peldaño, con el mismo gate de plan gratis=solo RA / plan completo=resto) se mantiene exactamente igual que antes, solo cambió cómo se VE.
Verificado en navegador a 375px con captura real: Peldaño RA se ve con 2 pasos ya completados (estrella dorada) y el 3ro activo con el león flotando encima; Peldaño RE aparece abajo con sus 3 pasos bloqueados (candado); toqué el paso 3 (la oración "Toco la guitarra") y abrió la pantalla de práctica correcta. Sin errores de consola, sin desborde a 375px, tsc limpio.
Con esto se cerró la ronda completa de pulido que pidió el usuario en este tramo de la sesión (voz, mascotas más grandes, memorama con imágenes/niveles, cronómetro, cuenta regresiva, colores del mapa, y ahora la Escalera con el mismo estilo).
Próximo paso exacto: preguntar al usuario si hay más pulido pendiente, o si pasamos a la Sesión 7 (servicios externos reales: GitHub, Supabase, Vercel, Resend, dominio, Hotmart).

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO (4 ajustes más de pulido, todos hechos):
1. Praxias: cada ejercicio ahora tiene pantalla "¿Estás listo? Empezar" ANTES de arrancar — al tocar Empezar corre una cuenta regresiva 3, 2, 1, ¡Ya! y RECIÉN AHÍ empieza el cronómetro de duración y se reproduce la voz (antes arrancaban solmotos apenas se abría la pantalla). Estado nuevo `iniciado`/`cuenta` en `app/app/praxias/page.tsx`, se resetea automáticamente en cada cambio de ejercicio (incluyendo el "Siguiente" de la sesión anterior), así que cada ejercicio pasa por su propio 3-2-1.
2. Mapa de Islas: los círculos de los nodos desbloqueados pasaron de colores pastel (`-soft`, ej. `bg-brand-primary-soft`) a colores SÓLIDOS (`bg-brand-primary` etc.) — el usuario tenía razón, el pastel contra el fondo crema de la app casi no se distinguía ("beige con beige"). Ahora los nodos abiertos son dorado/turquesa/coral bien saturados, se ven a la distancia.
3. Memorama: pasó de un set fijo de 6 palabras a un banco de 10 por letra (`lib/memorama-data.ts`) con NIVELES progresivos (`PAREJAS_POR_NIVEL = [4,6,8,10]`) — empieza fácil con 4 parejas y cada vez que gana, el botón dice "Siguiente nivel: más cartas" y sube de nivel con MÁS parejas Y una selección ALEATORIA distinta del banco (nunca las mismas 6 de siempre). Al llegar al nivel máximo (10 parejas, banco completo) sigue permitiendo jugar de nuevo, ya sin más niveles.
4. Escalera: los pasos completados ahora muestran una ESTRELLA dorada rellena (ícono nuevo `IconStarFilled` en `components/app/icons.tsx`) en vez del checkmark verde de antes — tanto en la lista de peldaños como en la lista de pasos dentro de cada peldaño. Coherente con el lenguaje de "estrellas" que ya usa el resto de la app (⭐ del contador, Racha, Nivel).
BUG REAL ENCONTRADO (de mi propia prueba, no del código): al simular clics muy rápido en el memorama con esperas cortas (700ms) entre pares, algunos pares que SÍ combinaban se marcaban como error — investigado y confirmado que es una limitación de la VELOCIDAD DE MI SCRIPT DE PRUEBA (el clic remoto vía CDP no es instantáneo), no un bug del juego: repetido el mismo par con esperas más generosas (300ms+1000ms) combinó perfecto. Un niño tocando la pantalla a mano nunca va a clickear así de rápido, así que esto no afecta el uso real — se deja anotado por transparencia, no se "arregló" nada porque no había nada roto.
Verificado a fondo: tsc limpio, sin errores de consola, sin desborde a 375px. Probado en navegador con capturas reales (el panel volvió a mostrarse esta vez): el 3-2-1 se vio literalmente contando "3,2,1,¡Ya!" antes de que el cronómetro arrancara; los nodos del mapa ahora se ven claramente dorados contra el fondo crema; el memorama subió de "Nivel 1 · 4 parejas" a "Nivel 2 · 6 parejas" tras ganar, con palabras nuevas random (salió "toro", que no estaba en el set original); la Escalera muestra estrellas doradas en los pasos completados.
Próximo paso exacto: preguntar al usuario si hay más ajustes de pulido, o si ya podemos pasar a la Sesión 7 (servicios externos reales: GitHub, Supabase, Vercel, Resend, dominio, Hotmart) que es lo próximo en la Secuencia Maestra.

⏸️ CHECKPOINT — Sesión 6 SIGUE EN CURSO (después de que el usuario dio por completa la lista del video de Duolingo, pidió 6 mejoras más de golpe). TODO HECHO:
1. Praxias: al terminar un ejercicio con "Ya lo intenté", el botón cambia a "Siguiente" y salta DIRECTO al próximo ejercicio de la secuencia completa (ya no hay que volver a la lista a elegir) — usa `TODAS_LAS_PRAXIAS` (orden plano de los 9) vía `siguientePraxia()`. En el último ejercicio de todos, el botón dice "¡Terminaste todos! Volver" y manda al listado principal.
2. Cada ejercicio de Praxias ahora tiene un cronómetro visual (⏱ Ns, cuenta regresiva de 15-20s según el ejercicio, campo nuevo `duracionSeg` en `lib/praxias-data.ts`) — es solo informativo, el botón "Ya lo intenté" sigue funcionando aunque el tiempo no haya llegado a 0 (no se le puede negar el paso a un niño impaciente).
3. La voz automática YA estaba hecha desde antes en esta sesión (Praxias/Sonidos/Escalera) — se confirmó que sigue funcionando, no hizo falta tocar nada.
4. Se agregó la foto del León/Pirata (`components/app/Mascota.tsx`, ya existente) como "compañero" arriba de cada ejercicio de Praxias, con el texto "El León/Pirata lo hace contigo". ⚠️ LÍMITE explicado al usuario: no se puede generar una animación real mostrando el movimiento de la lengua paso a paso — no hay herramienta de generación de video/animación disponible en esta sesión.
5. El Mapa de Islas (`app/app/page.tsx`) se reescribió por completo: ya NO muestra 4-6 "islas" grandes por sección, sino UN CAMINO CONTINUO con CADA ejercicio individual como su propia estrella/nodo (9 Praxias + 4 Sonidos + Memorama R + 15 Escalera R + 15 Escalera L + Memorama L = 45 nodos), agrupados bajo encabezados de sección, con desbloqueo estrictamente secuencial (cada nodo exige que el anterior esté completo, más el filtro de plan gratis/pago ya existente: Praxias+Sonidos+Memorama R+RA son gratis, el resto pide el plan completo). Se simplificaron los nodos (sin etiqueta de texto debajo, solo emoji+candado+check) porque con 45 nodos un texto largo por nodo saturaba la pantalla. Tocar un nodo desbloqueado lleva a la pantalla de esa SECCIÓN (Praxias/Sonidos/Escalera/Escalera-L/Premios) — OJO: no hay "deep link" al ejercicio exacto dentro de la sección todavía, eso quedó fuera de alcance por tiempo.
6. El memorama (`/app/premios`) ahora muestra un emoji-dibujo de cada palabra en la carta volteada, no solo el texto (🚗 carro, 🐶 perro, etc. — `lib/memorama-data.ts` pasó de `string[]` a `{palabra, emoji}[]`). ⚠️ LÍMITE explicado: son emojis, no fotos reales, por la misma razón de las imágenes de antes (no hay generador de imágenes disponible).
Bug real encontrado y corregido en el camino: el encabezado "PRAXIAS" quedaba tapado por la mascota flotante en la primera sección — se corrigió aumentando el espacio de arriba (`PAD_TOP`) y achicando un poco la mascota flotante (76px, antes 92-110px según la pantalla).
Verificado a fondo: tsc limpio, sin errores de consola, sin desborde horizontal a 375px (el camino ahora mide ~4700px de alto, es normal que haya mucho scroll — así es Duolingo de verdad). Se probó con clics reales: el auto-avance de Praxias saltó correctamente de "El Chasquido de Caballo" a "La Ola" con su propio cronómetro reiniciado; el conteo de nodos por sección se verificó por DOM (9/4/1/15/15/1, los 45 exactos); con progreso en cero solo el primer nodo (Praxias 1) aparece desbloqueado, todo lo demás con candado — la cadena secuencial funciona. El memorama mostró "🚗 carro" al voltear una carta.
Nota de sesión: el panel de vista previa del usuario estuvo "no visible" varias veces en este tramo (error de la herramienta, no del código) — toda la verificación de esta sección se hizo por texto de página / DOM / localStorage en vez de screenshots, igual de confiable para confirmar lógica aunque no reemplaza ver el diseño con los ojos.
Pendientes sueltos (ninguno bloqueante): (a) deep-link directo desde el Mapa al ejercicio exacto (hoy solo lleva a la sección); (b) el usuario nunca confirmó cómo sonó la voz nueva en su propio dispositivo; (c) el revisor-visual sigue sin poder puntuar (mismo límite técnico de guardado de capturas de toda la sesión).
Próximo paso exacto: el usuario mencionó que "no carga la vista previa" en un mensaje de este tramo — se confirmó que el servidor y el código están bien (aviso técnico, no de producto), pero vale la pena preguntarle de nuevo si ya puede ver el panel antes de seguir agregando cosas a ciegas.

⏸️ CHECKPOINT — Sesión 6 EN CURSO. HECHO: Mapa de Islas (`app/app/page.tsx`) rediseñado como sendero estilo Duolingo (curva SVG punteada + tramo recorrido en color sólido, nodos circulares con sombra 3D, mascota flotante animada sobre el nodo actual, entrada escalonada) y el bug de personalización QUEDÓ CORREGIDO: nombres de isla, emojis y mascota ahora cambian de verdad entre león/pirata según `p.interes` (antes solo /app/sonidos lo hacía). Verificado: tsc limpio, dev sin errores de consola, probado en navegador a 375px con datos reales en ambos temas (pirata/plan completo y león/plan gratis) — lock/unlock, progreso X/Y y banner de plan gratis se ven correctos. `npm run build` se colgó (timeout de 3 min, probablemente por Turbopack + servidor dev corriendo a la vez) — pendiente reintentar build en limpio. El subagente revisor-visual NO pudo puntuar (exige una imagen en disco y no encontré forma de persistir el screenshot del navegador a archivo en esta sesión) — quedó verificado visualmente por mí en vivo pero SIN el puntaje /40+/20 independiente; pendiente resolver el guardado de capturas o pedir al usuario una captura de su celular si quiere el puntaje formal antes de dar esto por 100% cerrado.
HECHO TAMBIÉN: Escalera Fonética reconstruida de punta a punta — pasó de 5 palabras sueltas a 5 peldaños (RA/RE/RI/RO/RU) × 3 niveles cada uno (sílaba de calentamiento → palabra → ORACIÓN completa) = 15 niveles reales, navegación de 3 pisos (lista de peldaños → lista de 3 pasos del peldaño → pantalla de práctica con el micrófono escuchando mientras el niño habla, igual mecánica que antes vía `useRugidoDetector`, reutilizada sin cambios). Progresión secuencial real: cada nivel se desbloquea solo al completar el anterior dentro del mismo peldaño (antes no existía esa secuencia, era una sola palabra por peldaño). Se creó `lib/escalera-data.ts` con los datos y helpers (`nivelPorId`, `TOTAL_NIVELES_ESCALERA`). Se simplificó `lib/progress.ts`: se eliminó el campo `silabaActual` (ya no se necesitaba, el desbloqueo ahora se deriva 100% de qué ids de nivel están en `palabrasHechas`) y `marcarPalabraHecha` se reemplazó por `marcarNivelEscaleraHecho(idNivel)`. Se actualizó `/app/mama` para mostrar el texto legible de cada nivel completado (antes mostraba la palabra cruda; ahora resuelve el id contra `nivelPorId`) y `/app` (Mapa de Islas) para calcular el total de la Escalera dinámicamente (15, ya no un 5 fijo). Las 4 palabras ya prometidas en el copy de venta (carro, perro, ferrocarril, guitarra) se conservaron dentro de los niveles nuevos. Verificado en navegador (dos pestañas, con y sin datos sembrados): tsc limpio, navegación de los 3 pisos funciona, el fallback "no pudimos usar el micrófono, pero igual anotamos tu intento" sigue marcando el nivel como hecho, el candado del plan gratis (solo RA desbloqueado) sigue funcionando, Mamá muestra los chips con texto legible y el conteo de estrellas/ejercicios sube correctamente.
Por decisión EXPLÍCITA del usuario se REDUJO el alcance de esta sesión, en este orden: (1) Mapa de Islas + bug de personalización — HECHO. (2) Escalera Fonética multinivel con oraciones — HECHO. Quedan pendientes para una siguiente vuelta: (3) más contenido/niveles en Praxias y Sonidos; (4) sección "Racha" — el usuario pidió inicialmente una tabla comparando con otros niños, pero al advertirle que eso exige backend/cuentas (cambio grande de arquitectura, hoy todo es local en el celular) aceptó que por ahora sea una RACHA/nivel del propio niño, sin comparar con otros usuarios reales (evita fabricar prueba social falsa); (5) secciones nuevas para practicar otras pronunciaciones tras dominar la R.
HECHO TAMBIÉN (3): Praxias y Sonidos ampliados + AUDIO en las indicaciones para niños que aún no leen. Praxias pasó de 5 ejercicios sueltos a 9 ejercicios en 3 niveles progresivos con desbloqueo secuencial (mismo patrón que la Escalera): "Calentamiento" (caballo/ola/reloj, siempre abierto), "Fuerza y Puntería" (pintor/columpio/serpiente nueva), "Casi Listos para la R" (moto/ardilla/elevador nuevas — ejercicios reales de precalentamiento para la vibración de la punta de la lengua, precursor directo de la R vibrante). Sonidos pasó de 2 a 4 sonidos por modo (León y Pirata), con etiqueta "Nivel N" visible. Datos de Praxias y Sonidos se extrajeron a `lib/praxias-data.ts` y `lib/sonidos-data.ts` (mismo patrón que `lib/escalera-data.ts`), y el Mapa de Islas ahora calcula sus totales (9 y 4) desde esos archivos en vez de números fijos.
AUDIO: se creó `hooks/useHablar.ts` (usa `SpeechSynthesisUtterance` del navegador, 100% local, coherente con la promesa de privacidad — nunca sale del dispositivo) y `components/app/BotonEscuchar.tsx` (botón de bocina reutilizable). Se agregó a las 3 pantallas de práctica (Praxias, Sonidos, Escalera): botón de "escuchar" junto al texto/instrucción + reproducción automática una vez al abrir cada pantalla, pensado para niños de 4-7 años que aún no saben leer bien.
Verificado: tsc limpio, probado en navegador (pestaña nueva, sin caché de HMR viejo): Praxias muestra los 3 niveles con candado correcto, el detalle de un ejercicio muestra instrucción + botón de bocina; Sonidos muestra los 4 niveles por modo con el botón de bocina en el detalle; Escalera con el botón de bocina en los 3 tipos de paso (sílaba/palabra/oración). Sin errores de consola reales (hubo un error de compilación de Turbopack que quedó pegado en el buffer del navegador de una edición anterior — confirmado con los logs del servidor que las últimas peticiones son 200 limpias, no es un error real).
HECHO TAMBIÉN (4): sección "Racha" — pantalla nueva `/app/racha` + entrada en la barra de navegación inferior (ahora 5 ítems: Mapa/Racha/Escalera/Premios/Mamá) + un chip de racha 🔥 junto a las estrellas en el encabezado del Mapa de Islas (enlaza a la pantalla). Muestra: racha actual de días consecutivos, mensaje de estado (ya practicó hoy / racha en riesgo si no practicó hoy pero sí ayer / invitación a empezar), una fila de los últimos 7 días con flamita en los días activos, mejor racha histórica, total de ejercicios, y un "Nivel" del propio niño (Cachorro→Explorador→Rugido Fuerte→Rey de la Selva→Leyenda del Rugido en modo León; Grumete→Marinero→Pirata Valiente→Capitán→Leyenda de los Mares en modo Pirata) calculado por umbrales de ejercicios totales — es SOLO el progreso del propio niño, nunca compara con otros niños reales (así se resolvió la ambigüedad original de "ver en qué número va cada niño", ver checkpoint anterior).
Cambios técnicos en `lib/progress.ts`: nuevo campo `diasActivos: string[]` (fechas que el niño practicó), las 3 funciones `marcar*Hecha` ahora registran el día de hoy; nuevas funciones puras `calcularRacha()` y `calcularNivel()`.
BUG REAL ENCONTRADO Y CORREGIDO EN VERIFICACIÓN: `calcularRacha` daba un día menos de los que debía (probado con 5 días seguidos reales, mostraba 4). Causa raíz: `new Date("YYYY-MM-DD")` parsea como medianoche UTC, pero el código leía luego los campos con getters LOCALES (`getDate()`/`getMonth()`) — en husos horarios negativos (todo LATAM) eso corre la fecha un día hacia atrás. Se corrigió centralizando TODO el parseo de fechas en un helper local-seguro (`analizarISOLocal`, construye la fecha con `new Date(y, m-1, d)` en vez de parsear el string), sin mezclar nunca parseo UTC con lectura local.
Verificado: tsc limpio, probado en navegador con 5 días consecutivos sembrados (racha=5 correcto tras el fix, mejor racha=5, nivel calculado correcto en ambos temas: "Rugido Fuerte" en León y "Pirata Valiente" en Pirata con el mismo progreso), chip de racha en el Mapa correcto, sin errores de consola. No pude tomar un screenshot final (el panel de vista previa no estaba visible en ese momento del lado del usuario) — verificado por texto de página en su lugar, igual de confiable para esta pantalla.
HECHO (pedido nuevo, fuera de la lista original del video): (a) VOZ — se corrigió `hooks/useHablar.ts` para elegir explícitamente una voz en español con heurística de nombre femenino (Helena/Sabina/Paulina/Mónica/Lucía/Elvira/"Google español"/etc.) en vez de dejar la voz por defecto del navegador (que sonaba mal/robótica); pitch bajado a natural (1.0, antes 1.05) y rate a 0.92. ⚠️ NO PUDE escuchar el resultado yo mismo — el navegador de pruebas de este entorno no tiene NINGUNA voz instalada (`speechSynthesis.getVoices()` devuelve vacío), así que la mejora es sólida en el código (heurística correcta, con manejo del evento `voiceschanged` para cuando las voces cargan tarde) pero el usuario debe confirmar en su propio celular/computadora cómo suena de verdad.
(b) MASCOTAS MÁS GRANDES: la mascota flotante del Mapa de Islas pasó de `text-4xl` a `text-7xl`; el círculo grande de Sonidos pasó de 160px a 192px con el emoji de `text-7xl` a `text-8xl`; los íconos de modo León/Pirata en Sonidos de `text-3xl` a `text-4xl`. Verificado sin overflow a 375px.
⚠️ LÍMITE IMPORTANTE que hay que avisar: el usuario compartió 2 imágenes de referencia (ilustración de niño pirata con loro, sticker de cachorro de león) pidiendo que sean "las animaciones" de cada sección. Esta sesión NO tiene ninguna herramienta de generación de imágenes disponible — solo se pudo agrandar el emoji existente (🦁/🏴‍☠️), NO crear ilustraciones personalizadas como las de referencia. Si el usuario quiere exactamente ese estilo ilustrado, necesita generarlas con una herramienta de imágenes (Midjourney/DALL-E/etc.) y compartir los archivos — ahí sí puedo integrarlas como imágenes estáticas reemplazando los emoji.
(c) MEMORAMA reemplazó el minijuego de reventar globos en `/app/premios`: 12 cartas (6 parejas) con palabras de la R (carro, perro, rana, risa, rueda, reloj), volteo de a 2, combina y queda destapada / no combina y se voltea sola. Se creó `lib/memorama-data.ts` con el banco de palabras organizado POR LETRA (hoy solo "R" — listo para cuando existan otras letras del punto 5). Se agregó `marcarMemoramaGanado()` a `lib/progress.ts` (nuevo campo `memoramaGanado: boolean`, +1 estrella la primera vez que gana, cuenta como actividad del día para la Racha) — antes el juego de globos PROMETÍA "+1 estrella" en el texto pero el código nunca la otorgaba (bug de honestidad, ya corregido de raíz al reconstruir el juego).
Verificado a fondo: tsc limpio, sin errores de consola. El panel de vista previa tampoco mostró captura visual esta vez (seguía sin desplegarse del lado del usuario), así que probé el memorama con clics reales simulados y lectura de localStorage: confirmé que una pareja que NO combina se voltea sola, que SÍ se puede ganar el juego completo, que al ganar aparece la pantalla de celebración, se suma la estrella (5→6), se marca `memoramaGanado=true` y se registra el día en la racha — los 3 casos (parcial, error, victoria) quedaron probados de verdad, no solo mirado.
Sigue pendiente de esta sesión (no bloqueante): el subagente revisor-visual sigue sin poder puntuar por falta de un mecanismo para guardar el screenshot del navegador a un archivo en disco.
ACTUALIZACIÓN: el usuario preguntó si podía usar DIRECTAMENTE las imágenes que había compartido en el chat (niño pirata con loro, cachorro de león) — se encontraron en `C:\Users\doran\Downloads\` (`17662623534312055.jpg` y `lion sticker.jpg`, ambas del mismo día) y SÍ se pudieron integrar de verdad: se copiaron a `superrre-app/public/mascots/leon.jpg` y `pirata.jpg`, se creó `components/app/Mascota.tsx` (usa `next/image`, tarjeta blanca redondeada `bg-surface-primary` + sombra — el fondo blanco de las fotos originales se funde casi perfecto con ese tono, casi no se nota el "cuadro") y se reemplazó el emoji por la foto real en: (1) la mascota flotante del Mapa de Islas (sobre el nodo actual, con la animación de flotar ya existente, cuidando NO mezclar el transform estático de centrado con el transform animado — mismo patrón de dos capas que ya se usaba en el resto del mapa) y (2) la pantalla de inicio del Memorama (antes de tocar "Jugar"). NO se tocó el círculo grande interactivo de Sonidos (es un botón funcional con estados de grabación/pulso, no solo decorativo — cambiarlo es un rediseño aparte) ni los íconos pequeños de 64px o menos (una foto detallada no se lee bien tan chica, ahí se dejó el emoji). Verificado en navegador a 375px en los dos temas (León y Pirata): las fotos se ven grandes, nítidas, sin recortes raros ni desbordes, sin errores de consola. Esta vez SÍ hubo panel de vista previa visible y se pudo capturar pantalla real.
HECHO (5) — ÚLTIMO PUNTO DEL VIDEO DE DUOLINGO, LISTA COMPLETA: se agregó la letra L como segunda pronunciación, desbloqueada SOLO al dominar la R por completo (los 15 niveles de la Escalera de la R). Cambios: `lib/escalera-data.ts` pasó de un array plano a `ESCALERA_POR_LETRA: Record<letra, grupos>` (R con su contenido de siempre sin cambios + L nueva: LA/LE/LI/LO/LU × 3 niveles cada una — lápiz/leche/libro/loro/luna, con oraciones reales, ej. "El loro habla mucho"), más helpers `totalNivelesDe`, `nivelesHechosDe`, `letraCompleta` (así cualquier letra futura se suma con la misma forma, sin tocar el resto del código). La UI de la Escalera se extrajo a un componente compartido `components/app/EscaleraLetra.tsx` (evita duplicar la pantalla de 3 pisos) usado por `/app/escalera` (letra R, con el gate de plan gratis de siempre) y la ruta NUEVA `/app/escalera-l` (letra L, todos los peldaños abiertos porque llegar ahí ya implica haber pagado y terminado la R). El Mapa de Islas ahora tiene una 5ª isla "Selva de la L"/"Isla de la L" (🌙 león / 🦜 pirata — ligados a las palabras "luna" y "loro" del propio banco), visible pero bloqueada con candado hasta dominar la R (mismo patrón visual que Premios/Escalera ya bloqueados — no aparece de la nada, motiva sin ser aire muerto). El Memorama (`/app/premios`) ahora tiene banco de palabras L (`lib/memorama-data.ts`) y, cuando L está desbloqueada, muestra pestañas "Letra R"/"Letra L" para elegir cuál jugar. `lib/progress.ts`: `memoramaGanado: boolean` se generalizó a `memoramasGanados: string[]` (una estrella por CADA letra que gane, no solo la primera vez en general).
Verificado a fondo en navegador (con datos sembrados de R 100% completa): la isla de la L aparece desbloqueada con 0/15, la Escalera de la L abre los 5 peldaños sin candado, la progresión sílaba→palabra→oración dentro de un peldaño sigue siendo secuencial igual que en R, se completó un nivel de verdad (LA-0) y quedó guardado (estrella +1, chip legible en Mamá junto a los de la R), el Memorama mostró las pestañas R/L, cambiar a L actualizó el título, y se ganó el memorama de L completo (estrella +1, `memoramasGanados` quedó con "L" sin tocar el de R). Sin errores de consola, sin desborde horizontal a 375px con la 5ª isla agregada. tsc limpio.
CON ESTO QUEDA COMPLETA TODA LA LISTA que el usuario pidió tras ver el video de Duolingo al inicio de la Sesión 6 (mapa estilo Duolingo + bug de personalización, Escalera con niveles y oraciones, más contenido en Praxias/Sonidos, audio en las indicaciones, Racha, fotos reales de los personajes, y ahora la sección para practicar otra letra tras dominar la R).
Pendientes sueltos que quedaron abiertos en el camino (ninguno bloqueante): (a) el usuario nunca confirmó cómo sonó la voz nueva en su propio dispositivo — preguntar; (b) el círculo grande interactivo de Sonidos sigue en emoji, no en foto (decisión de alcance, no error); (c) el subagente revisor-visual sigue sin poder puntuar por la limitación de guardado de capturas de esta sesión — toda la verificación de esta sesión fue funcional (texto de página, DOM, localStorage) en vez de visual.
Próximo paso exacto: la lista original del video está 100% cumplida — preguntar al usuario qué quiere hacer ahora (¿revisar todo junto en su celular? ¿pasar a Sesión 7 de servicios externos reales — GitHub/Supabase/Vercel/Resend/dominio/Hotmart, que es lo próximo en la Secuencia Maestra? ¿o algo nuevo?).

⏸️ CHECKPOINT ANTERIOR (Sesión 6, antes de codear) — rediseño estilo Duolingo del Mapa de Islas, aún SIN código escrito. El usuario compartió un video de referencia de Duolingo y pidió, en sus palabras: (1) Mapa de Islas con forma de camino/sendero tipo Duolingo, con nodos animados paso a paso; (2) personalización REAL según `p.interes` (leon/pirata) — hoy SOLO /app/sonidos respeta el interés elegido en el onboarding, el Mapa de Islas y el resto de la app quedan genéricos/leon-por-defecto — esto es un bug a corregir, no una feature nueva; (3) cada sección con VARIOS niveles de pronunciación de la R usando ORACIONES completas (no solo palabras sueltas), con el micrófono escuchando MIENTRAS el niño habla (estilo ejercicios de habla de Duolingo) — la base técnica sigue siendo `useRugidoDetector` (detección de volumen/sonido local, NUNCA reconocimiento de pronunciación real ni nube — restricción de producto ya decidida, no se reabre); (4) más banco de palabras/oraciones; (5) una sección para ver "en qué número va cada niño" — AMBIGUO, pendiente de interpretar con el usuario (probablemente ranking/nivel del propio niño, NO un leaderboard social falso — el SO prohíbe fabricar prueba social/otros usuarios inexistentes); (6) todas las secciones (Praxias/Sonidos/Escalera) hoy son demasiado planas (Escalera=5 niveles de 1 palabra c/u, Sonidos=2 por modo, Praxias=5 sin niveles) — deben tener progresión real; (7) secciones NUEVAS post-dominio-de-la-R para practicar otras pronunciaciones.
Decisión de esta sesión: se analizó el video vía frames pero se cortó por límite de tokens — se decidió NO seguir extrayendo frames (la descripción textual del usuario + el patrón conocido del mapa-sendero de Duolingo ya es suficiente para diseñar) y proceder directo a plan + implementación. Archivo temporal `_tmp-duo.mp4` y frames de scratch ya borrados.
Próximo paso exacto: presentar al usuario un plan corto (toca >5 archivos: lib/progress.ts, app/app/page.tsx, escalera, sonidos, praxias, + posible nueva sección de nivel/número y nueva sección post-R) antes de codear, y aclarar con el usuario el significado exacto de "en qué número va cada niño" si no queda claro solo. Luego implementar capa por capa verificando (tsc/build/dev) antes de avanzar, terminando con screenshot 375px + subagente revisor-visual antes de declarar listo.

⏸️ CHECKPOINT ANTERIOR — Sesión 5 CERRADA: app interna construida y probada de punta a punta. 6 pantallas nuevas bajo /app (con AppNav inferior: Mapa/Escalera/Premios/Mamá): (1) /app — Mapa de Islas (hub con racha de estrellas, 4 nodos con estado bloqueado/desbloqueado real); (2) /app/praxias — 5 ejercicios de gimnasia de lengua con instrucción + auto-reporte "Ya lo intenté"; (3) /app/sonidos — Modo León/Pirata con detección real de micrófono (hook useRugidoDetector extraído y reutilizado del onboarding); (4) /app/escalera — Escalera Fonética RA-RU con palabras reales (carro/perro/rana/ferrocarril/guitarra), gate free=solo RA vs completo=todas; (5) /app/premios — minijuego de reventar globos, se desbloquea tras 2 ejercicios Y plan completo; (6) /app/mama — resumen de progreso + upsell si plan free. Progreso persistido en localStorage vía lib/progress.ts con patrón useSyncExternalStore (reactivo entre pantallas). LoginForm ahora escribe el progreso inicial (nombre/plan/interés) y tiene una salida "modo de prueba" honesta hacia /app (el magic link real llega con Resend en Sesión 6).
BUGS REALES ENCONTRADOS Y CORREGIDOS EN VERIFICACIÓN: (a) bucle infinito de React por getSnapshot sin cachear en useSyncExternalStore — corregido con caché a nivel de módulo; (b) cuando el micrófono fallaba, el texto decía "igual anotamos tu intento" pero el código nunca llamaba a onDetectado() — corregido en /app/sonidos y /app/escalera para que el fallback SÍ registre el intento, cumpliendo lo que el copy promete. Verificado con datos reales en el navegador (Mateo, plan completo): estrellas suben de 1→2→3, Escalera avanza de silaba, Premios se desbloquea exactamente al segundo ejercicio, minijuego funciona. Build + typecheck + eslint limpios, sin overflow a 375px, consola sin errores en pestaña limpia / Siguiente acción exacta: proponer Sesión 6 (servicios externos reales: GitHub, Supabase, IA si aplica, Vercel, Resend, dominio, Hotmart)

## Qué es esta app (3 líneas máximo)
App gamificada en español para que niños de 4-7 años destraben la pronunciación de la letra R (rotacismo), mediante la mascota animada "el León" que muestra dónde poner la lengua, ejercicios de praxias linguales, sonidos/onomatopeyas con filtros AR y una escalera fonética progresiva. Dirigida a padres LATAM clase media/media-alta. Monetización: freemium con pago único ($19.99 USD) para desbloquear todo — sin suscripciones.

## Promesa central
"Ayudo a mamás de niños de 4 a 7 años con dificultad para decir la R a lograr que sus hijos suelten la lengua y pronuncien palabras como 'carro' y 'perro' con claridad en 15 días, sin tener que pelear todas las tardes con ejercicios aburridos frente al espejo ni pagar fortunas en terapias."

## Reporte de validación (Sesión 1) — YA APORTADO POR EL USUARIO (RESUMEN FINAL validado, no se re-valida)
- Veredicto: Excelente oportunidad — nicho desatendido en español (educación infantil + fonoaudiología)
- Apps de referencia: Speech Blubs (cobros no autorizados $59.99, difícil cancelar), Pronunciar la R / Bzngr (UI anticuada, aburrida), Writing Wizard/LetterSchool (foco en trazado, no en articulación)
- Lo que los usuarios odian de la competencia (nuestra oportunidad): cobros/suscripciones opacas y no autorizadas; apps repetitivas que aburren rápido; funciones ocultas tras muros de pago progresivos
- Brecha LATAM confirmada: sí — mercado hispanohablante saturado de apps de trazado/estimulación general, pero sin herramienta enfocada 100% en destrabar la R con gamificación moderna y pago justo
- Precio de referencia del mercado: $14.99/mes o $59.99/año (competencia) vs. nuestro pago único $19.99

### Segunda validación independiente (verificada por el agente vía búsqueda web, 2026-08-05)
- Prevalencia real del problema: 30% de niños de 4-5 años aún no pronuncian bien la R (fuentes clínicas de logopedia)
- Prueba de pago en el nicho: app de speech therapy similar (en inglés) alcanzó 5,000+ clientes y $300K ARR bootstrapeada — mercado global de speech therapy apps for kids ~$1.35B en 2024, creciendo 12.9%/año
- Único competidor directo en español (Pronuncia con Sarahí, UNAM): técnicamente correcto pero académico/aburrido, 3.0★ con solo 2 reseñas — confirma la brecha de gamificación en español
- Apps de referencia para diseño/progreso: Speech Blubs (peer modeling — tomar; cobros tras cancelación — evitar), Articulation Station (sistema de progreso por niveles palabra→frase→historia, credibilidad clínica — tomar)
- Veredicto: Excelente oportunidad confirmada de forma independiente

## Avatar y venta (Sesión 1 — NO cambiar sin validar)
- FICHA-AVATAR.md: pendiente de crear formalmente en Sesión 1 (datos ya recolectados abajo)
- Resumen: Carolina, 34 años, mamá LATAM clase media/media-alta, hijo Mateo (5) con rotacismo · dolor #1: culpa/impotencia al ver a su hijo burlado en el colegio por decir "cawo" en vez de "carro" · deseo #1: escuchar a su hijo decir "carro"/"perro" con claridad y orgullo · nivel de consciencia: Problem & Solution Aware (4/5) · sofisticación de mercado: alta (ya probó espejo, YouTube, apps con cobro sorpresa)
- Objeciones clave: cobro sorpresa/suscripción oculta · miedo a que aburra en 3 días · "¿reemplaza terapeuta?" · privacidad de micrófono/cámara de su hijo
- Razones de compra dominantes: 1) Escapar del dolor mental (culpa/miedo al bullying) 2) Evitar esfuerzo (el padre no sabe enseñar la biomecánica) 3) Ahorrar dinero (pago único vs. suscripción trampa)
- Landing: pendiente — usará la ESTRUCTURA CANÓNICA de 10 secciones del 19

## Estrategia de monetización (Sesión 1 — NO cambiar sin validar)
- Modelo: Freemium con desbloqueo de pago único (decidido por el usuario en su investigación previa — se respeta, no se reabre)
- Justificación: el dolor #1 del avatar es la desconfianza a cobros recurrentes ocultos; un pago único transparente elimina la objeción de compra más fuerte del nicho
- Diseño del paywall: gratis = módulo de Praxias + Onomatopeyas iniciales; $19.99 USD pago único desbloquea Escalera Fonética completa + minijuegos + todos los niveles
- Trial: freemium sin tarjeta de crédito (no es trial temporizado, es acceso gratuito limitado por funciones)
- Pricing: $19.99 USD pago único (benchmark competencia: $14.99/mes o $59.99/año)

## Constitución del Producto (Sesión 1 — decidido con el usuario)
- Onboarding: el padre/madre configura primero (nombre y edad del niño) antes de entregar el celular — personaliza la experiencia y genera confianza antes del paywall
- Gate de pago: puerta de adulto (challenge simple, ej. mantener presionado / operación matemática fácil) ANTES de mostrar paywall o cobrar — evita compras accidentales de un niño de 4-7 años
- Límites éticos que la app NUNCA cruza:
  1. Nunca publicidad ni compras dirigidas al niño (solo el padre ve ofertas, tras el gate de adulto)
  2. Nunca sube audio/video fuera del dispositivo — voz y cámara del niño 100% locales, nunca a servidor
  3. Nunca usa culpa o presión emocional para vender — copy siempre en tono de aliento, nunca miedo parental

## Gamificación y retención (Sesión 3 — el loop central)
- Loop del hábito (Hooked): Gatillo notificación/rutina diaria → Acción imitar sonido/video corto → Recompensa filtro AR + stickers/fiesta visual → Inversión progreso en Escalera Fonética
- Mecánicas elegidas: minijuego de recompensa (reventar globos/estrellas) desbloqueado cada 2 ejercicios; progreso visible en Escalera Fonética (RA-RE-RI-RO-RU)
- Primera victoria que celebra el onboarding (<60s): "El Rugido del León" — imita el sonido frente al micrófono y activa filtro de León animado
- Notificaciones de re-enganche: pendiente de diseñar en Sesión 4

## Secuencia maestra de construcción (NO saltar)
- Estado de la secuencia: Sesión 5 cerrada — lista para Sesión 6 (servicios externos)
- Ruta aprobada: `/` → `/onboarding` (incluye paywall y gate de adulto embebidos) → `/login` → `/app`
- Landing: construida y verificada — protagonista: el Espejo del León (primera victoria) — CTA primario: "Probar el Espejo gratis"
- Onboarding: construido y verificado — nombre → edad → dolor → reconocimiento → loading → primera victoria → celebración
- Paywall: construido y verificado (embebido al final del onboarding) — Gratis vs. Espejo Completo $19.99 pago único, con puerta de adulto antes del CTA de pago
- Login/Auth: construido y verificado (UI con mock de magic link — Supabase real se conecta en Sesión 6)
- App interna: construida y verificada — secciones: Mapa de Islas (hub), Praxias, Sonidos (León/Pirata con mic real), Escalera Fonética, Premios, Mamá
- Servicios externos: pendiente — GitHub/Supabase/Vercel/Resend/dominio/Hotmart

## Puertas de etapa (aprobacion antes de avanzar)
- Landing: aprobada — verificada en navegador con el usuario a lo largo de varias iteraciones
- Onboarding: aprobada — probada de punta a punta en navegador (nombre/edad/dolor/reconocimiento/loading/victoria/celebración)
- Paywall: aprobada — probada en navegador, personalización con nombre confirmada
- Login/Auth: aprobada (UI) — mock de magic link probado (estados enviando/enviado/reenvío)
- App interna: aprobada — probada de punta a punta en navegador con progreso real (estrellas, desbloqueos, minijuego)
- Servicios externos: bloqueados
- Certificado /100: pendiente

## Decisiones técnicas (NO re-discutir sin pedirlo el usuario)
- Framework: Next.js (App Router) — decidido el 2026-08-04. Razón: la app necesita landing con SEO/conversión, API routes para webhook de Hotmart, y posible BFF si se usa IA — Next.js es el default del stack pineado (51) para este perfil.
- Stack: scaffold canónico de 51-STACK-PINEADO.md (Next 16, Tailwind v4, shadcn/ui, Supabase con @supabase/ssr)
- Features del MVP (orden de prioridad):
  1. Gimnasia de Lengua (Praxias) — 5 rutinas animadas guiadas por la mascota León (sin video de niños reales — decisión de Sesión 3, ver nota abajo)
  2. Modo Onomatopeyas y Sonidos — imitación con activación de micrófono + filtros AR/stickers
  3. Escalera Fonética (RA/RE/RI/RO/RU) — práctica progresiva de sílabas y palabras clave
  4. Minijuego de Recompensa — se desbloquea tras completar 2 ejercicios
- Qué NO construir todavía: reconocimiento de voz en la nube (usar detección local de audio/volumen), trazado de letras/caligrafía, paneles multi-paciente para clínicas
- Modelo de IA: ninguno requerido para el MVP — el filtro AR y la detección de voz/volumen son 100% locales en el dispositivo (sin llamadas a proveedores de IA), coherente con la promesa de privacidad y con "qué NO construir todavía" del resumen validado
- Base de datos (Supabase/Postgres, decisión de implementación — no reabrir): tablas `parents` (cuenta que paga, vinculada a auth.users), `children` (nombre, edad, vinculado a parent_id — el niño NUNCA tiene su propia cuenta/login), `exercise_progress` (child_id, ejercicio, fecha, completado), `purchases` (parent_id, estado, fecha, origen webhook Hotmart). RLS: un parent solo lee/escribe sus propios children y su propio purchases/progress vía child_id → parent_id
- Auth (Supabase Auth, decisión de implementación — no reabrir): login SOLO para el padre/madre (email/magic link o Google) — el niño interactúa desde el dispositivo ya logueado por el padre, sin credenciales propias. Coherente con COPPA (menor de 13 años nunca crea cuenta ni ingresa datos personales directamente)

## Sesiones completadas ✅
- Sesión 0 — Instalación del SO + git init — verificado 2026-08-04

## Sesión en progreso 🔧
- Sesión 3 — Página de ventas: proyecto Next.js creado en `superrre-app/` (scaffold de 51), tokens de FICHA-ARTE volcados a `globals.css`, landing con las 10 secciones canónicas construida (Hero con mini-demo interactivo del mecanismo, Problema, Agitación, Solución con "el Espejo del León" bautizado, App por Dentro con placeholders del carrusel, Oferta pago único $19.99 + plan gratis, Garantía "del Primer Rugido", FAQ, CTA final + PS, Footer legal). Páginas legales (/privacidad /terminos /reembolso) creadas como borrador. Build y typecheck verificados limpios.
- Mecanismo bautizado: "el Espejo del León" — aparece en hero, sección solución y oferta (consistente en los 3 lugares, regla dura de 19).
- Desviación informada: el modelo es pago único (no mensual/anual como default del 19) — la sección Oferta muestra plan Gratis vs. Pago Único $19.99, coherente con la decisión de monetización de Sesión 1.
- Cambio de mecanismo (post-cierre, a pedido del usuario): la investigación original priorizaba "peer modeling" (video de niños reales imitando el sonido) por su alto engagement documentado — pero el usuario no tiene niños disponibles para filmar ese contenido. Se reemplazó por la mascota animada "el León" (ilustración/animación, consistente con la dirección de arte ya aprobada) mostrando dónde poner la lengua. Se actualizó: Hero, Solución (los 3 pasos), metadata de la página, y el feature #1 del MVP (Praxias). Mantiene el mismo insight de la investigación (mostrar visualmente el movimiento oculto de la lengua) sin depender de video de niños reales.

## Próximas sesiones 📋
- Sesión 4: Onboarding, paywall y login (hoy /onboarding y /login son stubs "en construcción")

## Problemas conocidos ⚠️
- Carrusel de "La app por dentro" usa placeholders rotulados "Próximamente" (la app interna aún no existe) — pendiente reemplazar por screenshots reales al cerrar Sesión 5, según regla del 19.
- Footer legal usa email de contacto placeholder (hola@supererre.app) — el usuario debe confirmar/crear una cuenta de correo real antes de publicar.
- Páginas /privacidad, /terminos, /reembolso son borradores base — recomendable revisión legal local antes de publicar.

## Pendientes del usuario (acciones que el usuario debe hacer)
- [ ] Ninguno todavía — se avisará cuando lleguemos a cuentas/dominio/claves (Sesión 6+)

## Notas para la próxima sesión
- El usuario ya trajo el RESUMEN FINAL VALIDADO + Perfil de Cliente Ideal (Carolina/Mateo) + Propuesta de Valor con las 10 razones de Jim Edwards. NO re-validar la idea ni re-investigar mercado — ya está hecho. Ir directo a completar B3 (Constitución) y presentar el Plan Maestro B5.
- Verificar edad mínima de usuarios finales (niños 4-7) implica consideraciones COPPA/privacidad reforzadas — ya contemplado en el resumen (procesamiento 100% local de audio/cámara).
